# BE-02 - Distribution platform model

Programme: Publisher Services and Distribution Configuration
Repository: `thoth-pub/thoth`
Workflow: STANDARD
Base branch: `develop`
PR target: `develop`
Programme integration branch: None
Risk: HIGH
Owner: CTO
Specification approval authority: CTO
Specification approval evidence: GitHub pull-request record
Implementation authorization: separate and absent
Dependencies: P0-01 (CLOSED); ADR-0001 (APPROVED, merged); ADR-0002 (APPROVED, merged); ADR-0003 / `THOTH-DB-CTRL-02` (APPROVED, merged); ADR-0004 / ADR-01 (APPROVED AND REPOSITORY-AUTHORITATIVE, merged as `299b0eff`); ADR-0005 (APPROVED, merged as `5a8c27b1`); BE-01 (CLOSED, merged through PR #779)
Target branch name: `feature/publisher-services/be-02`

Authority condition: this record is repository-authoritative when this exact
content is reachable from the repository's authoritative integration branch
(`develop`). Live review, authorization and merge evidence is the GitHub
pull-request record and is not copied here.

This record carries no transient status field. Under ADR-0005 and
[`docs/engineering/AGENTS.md`](../../AGENTS.md) section 1.1, whether this
specification has been independently reviewed, whether the CTO has approved it,
and whether it has merged are live GitHub facts, not committed prose. Recording
them here would be false before those events and stale after them, and the
commit that corrected them would invalidate the exact-head review that justified
it. Section 23 defines the distinct lifecycle events; section 24 defines the
approval authority and what approval does and does not grant.

This document is the BE-02 bounded task specification required by
[`operating-model.md`](../operating-model.md) Gate 1 and root `AGENTS.md`
section 1, and follows
[`task-specification-template.md`](../task-specification-template.md) with
additional BE-02-specific sections where generic template prose would compress
the platform lifecycle.

Specification preflight: the read-only preflight that produced this document
verified `thoth-pub/thoth` `develop` at
`5a8c27b1b7c11a4f6bd26d459556468099f8c1f4`. That SHA is a specification
preflight record only. It is **not** an implementation base and confers no
authorization; the implementation base is verified afresh at implementation
time (section 22).

---

## 1. Objective

Add the inactive, additive backend foundation for publisher distribution-platform
configuration by implementing the closed `DistributionPlatform` domain enum, the
`publisher_distribution_platform` assignment relation, the assignment activation
lifecycle, code-owned exhaustive platform descriptors, linked OAPEN/DOAB
normalization, and the public GraphQL platform metadata and assignment queries.

BE-02 activates nothing. It performs no dissemination, creates no distribution
job, executes no production migration, creates no publisher assignment row,
changes no publisher UI, and adds no service-configuration mutation.

### 1.1 Risk rationale

BE-02 is HIGH risk because it:

1. **carries a canonical schema migration** - it creates a new PostgreSQL enum
   type and a new persistent table in the canonical domain, and must update the
   repository-authoritative `thoth-api/src/schema.rs` atomically under ADR-0003
   Architecture A;
2. **introduces new persistent publisher service configuration** - the
   assignment relation becomes the authoritative desired-state record for
   publisher distribution, replacing publisher-ID environment variables in later
   tasks;
3. **establishes linked-state transactional invariants** - the OAPEN/DOAB linked
   pair must never reach a supported one-sided enabled state, and a linked
   transition must be atomic and share one activation identity;
4. **adds a public API contract** - four public GraphQL surfaces with stable
   enum codes, deterministic pagination and count semantics that downstream
   repositories will pin;
5. **is depended on downstream by dissemination** - `DIS-01`, `DIS-02`,
   `MIG-01`, `BE-03`, `BE-04` and `EXP-01` all consume these semantics;
6. **could broaden publisher processing if assignment semantics are wrong** - a
   query that returned disabled rows, or a helper encoding an "all publishers"
   fallback, would later cause unintended external delivery.

BE-02 is not CRITICAL: it performs no destructive production migration, no
canonical data rewrite, no external publication, no mass redistribution and no
global security-boundary change. Uncertainty does not reduce controls
(`risk-classification.md`, "Escalation rules").

### 1.2 Required HIGH-risk controls

- this approved specification, independently reviewed and explicitly
  CTO-approved, before implementation;
- separate explicit CTO implementation authorization bound to a freshly verified
  exact `develop` SHA (section 22);
- HIGH or maximum practical implementation reasoning;
- independent cross-model review by an agent/model that did not implement the
  task, bound to the exact PR head;
- empty-database and representative populated-database migration evidence on
  disposable PostgreSQL only;
- failure-path, concurrency, authorization and non-exposure tests;
- compatibility assessment;
- rollout and rollback plans;
- initially inactive behaviour;
- explicit CTO merge authorization bound to the exact independently reviewed PR
  head, with a guarded merge (`--match-head-commit`);
- separate, later, explicit authorization for any production deployment,
  migration execution, backfill or activation.

Approval of this specification does not authorize implementation. Merge of the
implementation is not production activation.

---

## 2. Background and authority

Authoritative sources, in precedence order:

1. merged code, migrations and generated contracts on `develop`;
2. [ADR-0004 - Distribution platform inventory](../../decisions/ADR-0004-distribution-platform-inventory.md)
   (`APPROVED AND REPOSITORY-AUTHORITATIVE`) and the
   [final platform inventory](../../../publisher-services/platform-inventory.md)
   (`FINAL INVENTORY APPROVED AND REPOSITORY-AUTHORITATIVE`);
3. [ADR-0002 - Platform domain boundaries](../../decisions/ADR-0002-platform-domain-boundaries.md);
4. [ADR-0003 - Repository-authoritative Diesel schema contract](../../decisions/ADR-0003-repository-authoritative-schema-contract.md);
5. [ADR-0005 - Terminal merge evidence](../../decisions/ADR-0005-terminal-merge-evidence.md);
6. [ADR-0001 - Publisher package capability model](../../decisions/ADR-0001-publisher-package-capability-model.md);
7. the approved private `Publisher Services and Distribution Configuration -
   Technical Design and Implementation Plan`, Drive revision `3`, indexed in
   [`docs/engineering/design-references.md`](../../design-references.md);
8. this specification;
9. the [Publisher Services programme controls](../../../publisher-services/README.md);
10. the [BE-01 specification](BE-01.md) and its merged implementation
    (PR [#779](https://github.com/thoth-pub/thoth/pull/779)) as the closest
    precedent.

Programme tracker: [issue #765](https://github.com/thoth-pub/thoth/issues/765).
Issue #765 is an older programme-control mirror with a synchronization guard and
stale historical checklist state. **BE-02 must not modify issue #765.**

Where the approved design document differs from ADR-0004 or the current
committed task records, the repository evidence supersedes it. Specifically, the
design's provisional 15-value list in its section 5.2 (including a bare
`PROQUEST` and no `EX_LIBRIS_KB`, `JISC_NBK` or `DOAB`-adjacent resolution) is
**superseded** by the final 17-value ADR-0004 inventory in section 5 below.

### 2.1 Dependency state

The first five rows are durable repository facts, verified at the specification
preflight base and re-verifiable from `develop` at any time. The last two rows
name **gates**, not statuses: their live state is the GitHub pull-request record
and the CTO authorization record, and this file does not restate it.

| Dependency | Required state | Repository evidence |
|---|---|---|
| ADR-0004 / ADR-01 | `APPROVED AND REPOSITORY-AUTHORITATIVE`, merged | Merged through PR #783 as `299b0eff`; ADR-01 was architecture/evidence work only and implemented no BE-02 runtime code |
| Final platform inventory | Approved, not provisional | `FINAL INVENTORY APPROVED AND REPOSITORY-AUTHORITATIVE` |
| ADR-0003 Architecture A | Merged and authoritative | `thoth-api/src/schema.rs` manually maintained; no root `diesel.toml` |
| ADR-0005 | Authoritative on `develop` | Merged as `5a8c27b1` |
| BE-01 | Merged | PR #779; `thoth_package` enum and `publisher.subscription_package` exist; every publisher is `OASIS` |
| **Gate:** CTO approval of this specification | Required before implementation may be authorized | Live evidence: the GitHub pull-request record for this specification. Not recorded here |
| **Gate:** explicit CTO implementation authorization, bound to a freshly verified exact `develop` SHA | Required before `feature/publisher-services/be-02` may be created | Live evidence: the CTO authorization record. Not recorded here. Section 22 defines the sequence |

### 2.2 Current repository findings (verified at `5a8c27b1`; the implementing agent must refresh against its own exact base)

**Absence of BE-02.**

- No `docs/engineering/ai-delivery/tasks/BE-02.md` or `BE-02-SPEC.md`.
- No branch containing `be-02`; no BE-02 pull request.
- `DistributionPlatform` appears only in documentation (`CHANGELOG.md`, ADRs,
  task records, programme controls). **No runtime Rust, SQL, schema or GraphQL
  implementation exists.**
- `publisher_distribution_platform` appears nowhere in the repository.

**Migrations and schema.**

- Migration directories are `thoth-api/migrations/YYYYMMDD_v<major>.<minor+1>.0/`
  containing `up.sql` and `down.sql`, created by `make migration` (the Makefile
  target derives the date and bumps the workspace minor version). The most
  recent is `thoth-api/migrations/20260805_v1.7.0/` (BE-01). **Do not invent a
  directory name; use `make migration`.**
- Migrations are applied and reverted by the embedded runner: `cargo run migrate`
  and `cargo run migrate --revert` (full-history revert).
- PostgreSQL enum convention (BE-01, current): `CREATE TYPE public.<snake_case>
  AS ENUM ('SCREAMING_SNAKE', ...);` with schema-qualified type names and
  schema-qualified casts in defaults.
- UUID convention: `uuid`, with `DEFAULT public.uuid_generate_v4()` where the
  database owns generation (`uuid-ossp` extension is installed).
- Timestamp convention is mixed historically. Older tables (`publisher`,
  `issue`, `affiliation`) use `timestamp without time zone DEFAULT
  CURRENT_TIMESTAMP NOT NULL`; newer tables (`award`, `book_review`,
  `additional_resource`) use `timestamp with time zone DEFAULT CURRENT_TIMESTAMP
  NOT NULL`. `thoth-api/src/schema.rs` maps both to `Timestamptz` (an
  intentional physical-to-Rust alias preserved under ADR-0003).
- `updated_at` maintenance convention: a per-table
  `CREATE TRIGGER set_updated_at BEFORE UPDATE ON <table> FOR EACH ROW EXECUTE
  FUNCTION public.diesel_set_updated_at();`. That function sets
  `NEW.updated_at := current_timestamp` only when the row actually changed and
  `updated_at` was not explicitly set.
- Foreign-key convention: `ON DELETE CASCADE` (62 of 64 foreign keys; the
  remaining two are `ON DELETE SET NULL` for optional references).
- Partial-index convention is established and idiomatic, for example
  `CREATE UNIQUE INDEX location_uniq_platform_idx ON public.location USING btree
  (publication_id, location_platform) WHERE (NOT (location_platform =
  'Other'::public.location_platform));` and `CREATE UNIQUE INDEX
  publisher_zitadel_id_key ON public.publisher USING btree (zitadel_id) WHERE
  (zitadel_id IS NOT NULL);`.
- Check-constraint convention: named `CONSTRAINT <table>_<subject>_check CHECK
  (...)`, declared inline in `CREATE TABLE` or added by `ALTER TABLE`.
- Primary-key convention: every existing table has a single surrogate `uuid`
  primary key. Join tables (`issue`, `work_relation`) also carry a surrogate key
  plus a `UNIQUE` constraint on the natural pair. **No existing table uses a
  composite primary key**, so BE-02's approved `publisher_id + platform`
  composite primary key is a deliberate, specification-level departure (see
  section 6.2).
- Migration CI: `.github/workflows/run_migrations.yml` applies, reverts and
  reapplies the full chain on a disposable database.

**Rust model conventions.**

- Persisted enums use
  `#[cfg_attr(feature = "backend", derive(diesel_derive_enum::DbEnum,
  juniper::GraphQLEnum), graphql(description = "..."), ExistingTypePath =
  "crate::schema::sql_types::<Type>")]`, with per-variant
  `#[cfg_attr(feature = "backend", db_rename = "SCREAMING_SNAKE",
  graphql(description = "..."))]`.
- Non-persisted domain enums use `#[cfg_attr(feature = "backend",
  derive(juniper::GraphQLEnum), graphql(description = "..."))]` with no
  `DbEnum`.
- Both carry `#[derive(Debug, Clone, Copy, ..., Serialize, Deserialize,
  PartialEq, Eq, EnumString, Display)]`, `#[serde(rename_all =
  "SCREAMING_SNAKE_CASE")]` and `#[strum(serialize_all =
  "SCREAMING_SNAKE_CASE")]`.
- `ThothPackage` (BE-01) is the exact precedent, in
  `thoth-api/src/model/publisher/mod.rs`.
- Code-owned mappings are `const` slices plus an exhaustive `match self` with no
  wildcard arm (`ThothPackage::capabilities`).
- Model modules live under `thoth-api/src/model/<entity>/` as `mod.rs`,
  `crud.rs`, `policy.rs`, `tests.rs`, declared in `thoth-api/src/model/mod.rs`.
- The `Crud` trait provides the generic CRUD/list/count contract used by GraphQL
  CRUD entities; `crud_methods!` supplies `from_id`/`create`/`update`/`delete`.
  `update` wraps its write and history insert in `connection.transaction(...)`.
- Transactions use `diesel::Connection::transaction`. `thoth-api/AGENTS.md`
  section 5 requires database-enforced correctness for concurrent operations and
  lists row locks, unique constraints and check constraints as the approved
  tools.
- Time is `crate::model::Timestamp` (a newtype over `DateTime<Utc>`).
- Errors are `thoth_errors::ThothError` variants; the established convention is
  a dedicated stable variant per domain rule (e.g. `CanonicalLocationError`,
  `IssueImprintsError`, `ChapterIsbnError`).
- Ordering uses `apply_directional_order!`, which supports a primary expression
  **plus an optional always-ascending secondary tie-breaker**.

**GraphQL conventions.**

- One `QueryRoot` in `thoth-api/src/graphql/query.rs` with flat top-level
  fields; entity objects are implemented explicitly in
  `thoth-api/src/graphql/model.rs` via `#[juniper::graphql_object(Context =
  Context)]`. Simple code-owned value objects may use
  `#[derive(juniper::GraphQLObject)]` (precedent: `FileUploadResponse`,
  `UploadRequestHeader` in `thoth-api/src/model/file/mod.rs`).
- **Pagination is offset/limit, not cursor-based.** The established shape is
  `limit: Option<i32>` (`default = 100`), `offset: Option<i32>`
  (`default = 0`), an `order: Option<XOrderBy>` input, returning `Vec<T>`, with
  a separate sibling `xCount(...) -> i32`. There is no connection/edge/pageInfo
  type anywhere in the schema. **BE-02 must not introduce one.**
- Counts return `i32` via the documented `i64 -> String -> i32` conversion in
  `Crud::count` implementations.
- Nullability follows Rust: `Option<T>` is nullable, everything else
  non-nullable. Every field and argument carries a `#[graphql(description =
  "...")]`.
- Errors are `FieldResult<T>` with `.map_err(Into::into)` from `ThothResult`.
- Empty results are returned as empty `Vec`, never as an error.
- **`QueryRoot` read fields perform no authorization**: the entire GraphQL read
  API (including `publishers`, `publisher`, `publisherCount`) is anonymously
  accessible today. `crate::policy` is used by mutations and by protected
  surfaces, not by these public reads.
- `thoth-client/assets/schema.graphql` is generated at build time by
  `thoth-client/build.rs` and is **not** checked into the repository (only
  `thoth-client/assets/queries.graphql` is tracked). No committed generated-SDL
  update is therefore required by BE-02.

**Existing publisher surface.**

- `Publisher`, `NewPublisher`, `PatchPublisher`, `PublisherHistory`,
  `PublisherOrderBy`, `PublisherField`, `ThothPackage` and `PublisherCapability`
  are in `thoth-api/src/model/publisher/mod.rs`.
- `Publisher::all` / `Publisher::count` are in
  `thoth-api/src/model/publisher/crud.rs`. **`Publisher::all` applies no
  tie-breaker**, so its pagination is not deterministic under duplicate sort
  keys. BE-02 must not inherit that defect (section 7.3).
- The public GraphQL `Publisher` object is at `thoth-api/src/graphql/model.rs`
  and currently exposes no package or capability data. `Publisher.imprints` and
  `Publisher.contacts` are the precedent for a publisher-scoped child field.
- Database-backed tests live in `thoth-api/src/model/<entity>/tests.rs` and
  `thoth-api/src/graphql/tests.rs`, using the shared harness in
  `thoth-api/src/model/tests.rs` (`mod db`, `TEST_DATABASE_URL`, exclusive file
  lock, `execute_graphql`).

---

## 3. Explicit scope

The task must, subject to refreshed live code discovery at the implementation
base:

1. create the PostgreSQL `distribution_platform` enum type (section 5.1);
2. add the closed Rust `DistributionPlatform` enum with exactly the 17 ADR-0004
   values (section 5.2);
3. expose `DistributionPlatform` as a GraphQL enum with the exact stable codes
   (section 5.3);
4. create the `publisher_distribution_platform` table, its constraints, index
   and `updated_at` trigger, and update `thoth-api/src/schema.rs` manually and
   atomically in the same PR (section 6);
5. add the publisher/platform assignment model and query layer (section 6.4);
6. implement the activation lifecycle domain logic (section 7.1);
7. implement linked OAPEN/DOAB normalization (section 7.2);
8. implement code-owned exhaustive platform descriptors with compile-time
   exhaustiveness and no fallback arm (section 8);
9. implement the code-owned `BackCatalogueBehaviour` representation (section 8);
10. enforce `JISC_NBK` non-assignability in the supported domain write path
    (section 7.4);
11. add the public `distributionPlatformOptions` query (section 9.1);
12. add the public `Publisher.distributionPlatforms` field (section 9.2);
13. add the public `publishersByDistributionPlatform` query (section 9.3);
14. add the public `publisherCountByDistributionPlatform` query (section 9.4);
15. add the required `thoth-errors` variant for a non-assignable platform
    (section 10);
16. verify internal GraphQL client and generated-schema compatibility, and make
    any change that repository convention actually requires (expected: none -
    the generated SDL is untracked and `thoth-client/assets/queries.graphql`
    needs no new query);
17. add focused migration, model, descriptor, lifecycle, concurrency, GraphQL,
    authorization/non-exposure and regression tests (section 18);
18. produce the compatibility analysis (section 12);
19. update `CHANGELOG.md` under `## [Unreleased]` / `### Added`, referencing the
    implementation PR number, and complete the BE-02 implementation report from
    `docs/engineering/ai-delivery/implementation-report-template.md`;
20. reconcile the Publisher Services tracker
    (`docs/publisher-services/task-status.md`) only to the extent the current
    controls require a *material* repository update within the implementation
    task - the BE-02 row's `BLOCKED` state and dependency prose become
    materially incorrect once the implementation exists, which ADR-0005
    section 8 recognises as a legitimate in-task change. Do not add lifecycle
    metadata (review IDs, approval IDs, merge SHAs) to any committed file.

Likely implementation areas, to be confirmed against the exact base and **not**
treated as an allowlist:

```text
thoth-api/migrations/<new-version>/up.sql
thoth-api/migrations/<new-version>/down.sql
thoth-api/src/schema.rs
thoth-api/src/model/mod.rs
thoth-api/src/model/publisher_distribution_platform/mod.rs
thoth-api/src/model/publisher_distribution_platform/crud.rs
thoth-api/src/model/publisher_distribution_platform/tests.rs
thoth-api/src/model/publisher/crud.rs
thoth-api/src/graphql/model.rs
thoth-api/src/graphql/query.rs
thoth-api/src/graphql/tests.rs
thoth-errors/src/lib.rs
CHANGELOG.md
docs/publisher-services/task-status.md
docs/engineering/ai-delivery/implementation-reports/BE-02-implementation-report.md
```

The implementing agent must inspect the exact base, must name the actual
surfaces it changes in the implementation report, and must not add a path merely
to satisfy guessed architecture.

---

## 4. Non-goals

BE-02 must not implement, add, change or authorize any of the following.

**BE-03 (protected service configuration)**

1. `PublisherServiceConfiguration` type or `publisherServiceConfiguration`
   query;
2. owner/superuser protected configuration access;
3. the staff service-configuration report query;
4. `replacePublisherServiceConfiguration` or any other configuration mutation;
5. `publisher_service_configuration_history` or any service-configuration audit
   history;
6. the optimistic-concurrency (`expectedUpdatedAt`) public API.

**BE-04 (durable distribution jobs)**

7. `distribution_job`, `distribution_job_target`, `distribution_job_attempt`,
   `distribution_job_kind` or `distribution_job_status`;
8. worker claims, claim tokens, leases or worker roles;
9. complete/fail/retry/cancel operations;
10. automatic back-catalogue job creation of any kind.

**MIG-01 (audit and production backfill)**

11. production package or platform backfill;
12. legacy publisher mapping from environment variables, workflows or private
    configuration;
13. production assignment creation;
14. the no-job import implementation;
15. production audit execution.

**APP-01 and app work**

16. publisher UI or staff UI;
17. app-local linked-platform rules;
18. any GraphQL codegen or other change in `thoth-app`.

**DIS-01 / dissemination**

19. API publisher discovery in `thoth-dissemination`;
20. comparison mode;
21. environment-variable cutover;
22. uploader mapping or cutover.

**EXP-01**

23. the OCLC KBART publisher index endpoint.

**OAI-01**

24. OAI-PMH gating, and any change to `feature/oai-pmh-http`.

**Production**

25. deployment, release, production migration execution, publisher assignment
    activation, credential access, dissemination cutover, back-catalogue
    processing, automatic uploads or comparison-mode activation.

**Also explicitly excluded**

26. work-level distribution selection or the `ALL_PUBLISHER_PLATFORMS` /
    `SELECTED_PLATFORMS` / `NONE` model;
27. Thoth-managed source-file enforcement (ADR-0004 section 4.6 is recorded
    future architecture, not BE-02 work);
28. automatic withdrawals;
29. any new automatic update behaviour beyond recording existing descriptor
    metadata;
30. platform inventory changes, additions, removals or reconsideration of the 10
    ADR-0004 exclusions;
31. changes to the ADR-01 evidence ledger, evidence matrix or evidence counts;
32. `MetricPlatform` changes or any cross-domain mapping;
33. package-to-platform coupling in either direction;
34. unrelated ADR-0003 wording cleanup;
35. unrelated operating-model or historical control-record wording cleanup;
36. reintroduction of `diesel_cli`, `diesel print-schema` as canonical writer, a
    root `diesel.toml`, a schema synchronizer or any structural
    schema-generation subsystem;
37. a database platform-descriptor table or any second destination registry;
38. unrelated refactoring, and changes to any other repository.

---

## 5. The `DistributionPlatform` enum - binding

### 5.0 Final inventory

`DistributionPlatform` contains exactly these 17 stable values, in this
canonical order (ADR-0004 section 4.1):

```text
INTERNET_ARCHIVE
OAPEN
DOAB
SCIENCE_OPEN
CAMBRIDGE_UNIVERSITY_LIBRARY
CROSSREF
FIGSHARE
ZENODO
PROJECT_MUSE
JSTOR
EBSCO_HOST
PROQUEST_EBOOK_CENTRAL
GOOGLE_PLAY
BKCI
OCLC_KB
EX_LIBRIS_KB
JISC_NBK
```

No other value may be added by BE-02. The following are explicitly prohibited:

- an `OTHER` value;
- any fallback, catch-all, wildcard or default enum value;
- any `UNKNOWN` or `PROVISIONAL` value;
- a universal platform enum shared across domains;
- collapsing `DistributionPlatform` into `MetricPlatform`.

`DistributionPlatform` and `MetricPlatform` remain separate domains with no
name-based conversion, no shared GraphQL enum and no inferred mapping
(ADR-0002 sections 4.1, 4.2, 4.5).

Aliases and historical names resolve to stable codes and do **not** become
additional enum values. In particular: `Google Books` -> `GOOGLE_PLAY`;
`ProQuest` / `Ebrary` -> `PROQUEST_EBOOK_CENTRAL`; `ProQuest ExLibris` ->
`EX_LIBRIS_KB`; `JISC KB` / `Jisc KB` -> `JISC_NBK`. The complete alias table is
ADR-0004 section 4.2.

The 10 ADR-0004 exclusions remain excluded and must not be reconsidered by
BE-02: `EBSCO_KB`, `PROQUEST_SERIALS_SOLUTIONS_KB`, `OVERDRIVE`, `BDS_LIVE`,
`RNIB_BOOKSHARE`, `SCIELO_BOOKS`, `ZOTERO`, `THOTH`, `PUBLISHER_WEBSITE`,
`OTHER`.

If live ADR-0004 contradicts any value above, the implementing agent must stop
and report the conflict rather than choosing silently.

### 5.1 PostgreSQL enum

```text
Type name:  public.distribution_platform
Kind:       PostgreSQL ENUM
Values:     the 17 codes of section 5.0, uppercase SCREAMING_SNAKE_CASE,
            single-quoted, in the canonical order above
```

The type name `distribution_platform` is the identity named by the approved
design and by ADR-0002 section 4.1, and it matches the current repository
convention for PostgreSQL enum type names (`thoth_package`, `location_platform`,
`work_type`, `contact_type`). No materially different database identity is
required, so no approved stable identity is changed.

**Declaration order is binding.** PostgreSQL orders enum values by declaration
order, and BE-02 relies on that ordering for the deterministic ordering
guarantees in sections 9.1 and 9.2. The declaration order is the ADR-0004
section 4.1 inventory order.

**Creation order in `up.sql`:** create the type first, then the table that
references it, then constraints, then the index, then the trigger. This matches
the BE-01 migration shape.

**Down migration:** drop in reverse dependency order -

```sql
DROP TABLE IF EXISTS public.publisher_distribution_platform;
DROP TYPE IF EXISTS public.distribution_platform;
```

The trigger and index are dropped implicitly with the table. `IF EXISTS` follows
the BE-01 `down.sql` convention.

**Compatibility with existing databases:** the migration is purely additive. It
creates one new type and one new table, alters no existing table, adds no column
to an existing table, and creates no row. Existing databases are unaffected
except for the new, unused objects.

### 5.2 Rust enum

`DistributionPlatform` is a closed, exhaustive Rust domain enum with exactly 17
variants in the canonical order.

- **Diesel/PostgreSQL conversion:** `diesel_derive_enum::DbEnum` with
  `ExistingTypePath = "crate::schema::sql_types::DistributionPlatform"` and a
  per-variant `db_rename` giving the exact uppercase database label, exactly as
  BE-01 does for `ThothPackage`. The `sql_types::DistributionPlatform` struct is
  added to `thoth-api/src/schema.rs` with
  `#[diesel(postgres_type(name = "distribution_platform"))]`.
- **serde representation:** `#[serde(rename_all = "SCREAMING_SNAKE_CASE")]`, so
  serde emits and accepts exactly the stable codes.
- **Stable string representation:** `strum::Display` / `strum::EnumString` with
  `#[strum(serialize_all = "SCREAMING_SNAKE_CASE")]`, so `Display` and
  `FromStr` also use exactly the stable codes.
- **GraphQL representation:** `juniper::GraphQLEnum` (section 5.3).
- **Derives:** `Debug, Clone, Copy, Serialize, Deserialize, PartialEq, Eq,
  EnumString, Display` (plus `Hash` if the implementation needs it).
- **No `Default`.** Unlike `ThothPackage`, `DistributionPlatform` has no
  defensible default: a default would function as a fallback value. The
  implementation must not derive or implement `Default` for it.
- **Invalid-value failure semantics:** an unrecognised database label, serde
  value, `FromStr` input or GraphQL input value must fail. `DbEnum` conversion
  returns a Diesel deserialization error which surfaces as
  `ThothError::DatabaseError`; `FromStr` returns `Err`; an invalid GraphQL enum
  literal or variable fails Juniper's input coercion before the resolver runs.
  No path may substitute a default, a nearest match or a fallback.
- **No wildcard behaviour:** every `match` on `DistributionPlatform` in BE-02
  code must be exhaustive with no `_` arm.

Placement: the enum, the descriptor types and the assignment model belong in one
new module, `thoth-api/src/model/publisher_distribution_platform/`, declared in
`thoth-api/src/model/mod.rs`. The implementing agent may instead follow a
different placement that the live tree demonstrates is more idiomatic, and must
justify it in the implementation report; it must not scatter the enum,
descriptors and model across unrelated modules.

`Crud` must **not** be implemented for the assignment entity. `Crud` is the
generic contract for GraphQL CRUD entities with create/update/delete mutations,
which BE-02 explicitly does not add (BE-03 owns the mutation surface). BE-02
provides bespoke, narrowly scoped domain and query functions instead.

### 5.3 GraphQL enum

`DistributionPlatform` is exposed as a GraphQL enum with exactly the 17 stable
codes as its value names, carrying a type-level description and a per-variant
description. Values are the uppercase codes verbatim; no aliasing, no lowercase
form, no deprecated values, no fallback value.

Generated-schema consequence: `thoth-client/build.rs` writes
`thoth-client/assets/schema.graphql` at build time from the reachable Juniper
schema, and that file is untracked. Because BE-02 references
`DistributionPlatform` from reachable public query surfaces, it will appear in
the generated SDL; no committed generated artefact changes. The implementation
report must record the observed generated-SDL diff and confirm that no existing
type, field, input or enum value was removed or changed.

---

## 6. `publisher_distribution_platform` data model

### 6.1 Table

```sql
CREATE TABLE public.publisher_distribution_platform (
    publisher_id  uuid NOT NULL,
    platform      public.distribution_platform NOT NULL,
    enabled       boolean NOT NULL,
    activation_id uuid NOT NULL,
    enabled_at    timestamp with time zone NOT NULL,
    disabled_at   timestamp with time zone,
    created_at    timestamp with time zone DEFAULT CURRENT_TIMESTAMP NOT NULL,
    updated_at    timestamp with time zone DEFAULT CURRENT_TIMESTAMP NOT NULL,
    CONSTRAINT publisher_distribution_platform_enabled_state_check CHECK (
        (enabled AND disabled_at IS NULL)
        OR
        (NOT enabled AND disabled_at IS NOT NULL)
    )
);
```

The exact SQL text is the implementing agent's, but the types, nullability,
defaults, constraint semantics and names above are binding.

**The row-existence rule that makes those types correct.** A row in this table
exists **only because the platform has been enabled at least once** for that
publisher. The lifecycle (section 7.1) creates a row on `ABSENT -> ENABLED` and
on no other transition; `ABSENT -> DISABLED` is a no-op that creates nothing.
Therefore every persisted row has, by construction, an activation identity and
an activation start time, and those two columns are `NOT NULL`. There is no
"never activated" persisted state to represent.

**Settled decisions and their basis:**

| Question | Decision | Basis |
|---|---|---|
| `publisher_id` type | `uuid NOT NULL`, no default | matches `publisher.publisher_id`; the value always comes from an existing publisher |
| `platform` type | `public.distribution_platform NOT NULL` | the PostgreSQL enum of section 5.1 |
| `enabled` | `boolean NOT NULL`, **no database default** | a row must never be `NULL`-enabled. A `DEFAULT false` is deliberately **not** used: it would advertise that a valid row can be created without an activation, which the lifecycle forbids. Every insert is an activation and states `enabled = true` explicitly. Fail-closed is achieved by row *absence*, not by a disabled default |
| `activation_id` | `uuid NOT NULL`, **no database default** | every persisted row exists because of an activation, so the identity is always present. On the generation owner, see below |
| `enabled_at` | `timestamp with time zone NOT NULL` | records the start of the current activation, or of the most recently completed activation for a retained disabled row. Always present for the same reason |
| `disabled_at` | `timestamp with time zone`, **nullable** | this is the only genuinely optional lifecycle timestamp: it is `NULL` exactly while the assignment is enabled, and set exactly while it is disabled |
| `created_at`, `updated_at` | `timestamp with time zone DEFAULT CURRENT_TIMESTAMP NOT NULL` | the newer repository table convention (`award`, `book_review`, `additional_resource`); using `with time zone` makes the physical type agree with the `Timestamptz` mapping already used throughout `thoth-api/src/schema.rs`, rather than relying on the legacy `without time zone` alias carried by older tables. This is implementation-level typing consistent with current repository practice and changes no approved architecture |
| Primary key | `PRIMARY KEY (publisher_id, platform)` | the approved design's explicit requirement; it makes "one assignment row per publisher per platform" a database guarantee rather than an application check, which `thoth-api/AGENTS.md` section 5 requires. No surrogate key is added: a surrogate would permit duplicate `(publisher_id, platform)` pairs unless separately constrained, and BE-02 adds no CRUD mutation that needs one |
| Foreign key | `FOREIGN KEY (publisher_id) REFERENCES public.publisher(publisher_id) ON DELETE CASCADE` | the dominant repository convention (62 of 64 FKs); deleting a publisher must not leave orphan assignments, and assignments are configuration, not audit history. Its locking consequences are assessed in section 13.3 |
| `activation_id` generation owner | **the application (Rust), not the database** | a linked OAPEN/DOAB activation must write **one** activation identity to **two** rows in one transaction. A column `DEFAULT uuid_generate_v4()` would generate two different values, silently breaking the linked-identity invariant. The domain layer therefore generates one `Uuid::new_v4()` per logical activation and writes it to every row in that activation |
| `enabled_at` / `disabled_at` / `updated_at` generation owner | **the database transaction timestamp** (`CURRENT_TIMESTAMP` / `now()`), evaluated inside the single transition transaction | `CURRENT_TIMESTAMP` is transaction-start time and is therefore *identical* for every row written by one logical transition, which is exactly the determinism the linked pair requires. `clock_timestamp()` must not be used |
| `updated_at` maintenance | add `CREATE TRIGGER set_updated_at BEFORE UPDATE ON public.publisher_distribution_platform FOR EACH ROW EXECUTE FUNCTION public.diesel_set_updated_at();` | the established per-table repository convention; the function already exists and sets `current_timestamp` only when the row actually changed |

#### 6.1.1 Binding row-level invariant

The named check constraint enforces exactly one thing, because the `NOT NULL`
declarations already carry the rest:

```text
enabled  =>  disabled_at IS NULL
NOT enabled  =>  disabled_at IS NOT NULL
```

equivalently, `enabled` and `disabled_at IS NULL` are the same proposition. An
implementation may express this as the two-branch `CHECK` above or as the
equivalent `CHECK (enabled = (disabled_at IS NULL))`; the two-branch form is
preferred because it matches the repository's explicit named-`CHECK` style.

Taken together with the column declarations, the persisted states are exactly
two, and no other state is representable:

| Persisted state | `enabled` | `activation_id` | `enabled_at` | `disabled_at` |
|---|---|---|---|---|
| currently enabled | `true` | present | start of the current activation | `NULL` |
| retained disabled | `false` | the activation that was ended | start of that ended activation | when it ended |

The following states are **rejected by the database**, not merely discouraged:

- a row with no activation identity (`activation_id IS NULL`);
- a row with no activation start (`enabled_at IS NULL`);
- an enabled row that still carries a `disabled_at`;
- a disabled row with no `disabled_at`;
- a row with `enabled IS NULL`.

The previously specified constraint permitted the first three of these, and a
`DEFAULT false` on `enabled` implied that a never-activated disabled row was a
legitimate persisted state. Neither is consistent with the lifecycle, and both
are corrected here.

**How the lifecycle operations satisfy the invariant** (full state machine in
section 7.1; `T` is the transaction timestamp):

| Operation | Statement | Column values written |
|---|---|---|
| `ABSENT -> ENABLED` | `INSERT` | `enabled = true`, `activation_id` = newly generated, `enabled_at = T`, `disabled_at = NULL` |
| `DISABLED -> ENABLED` | `UPDATE` | `enabled = true`, `activation_id` = **newly generated**, `enabled_at = T`, `disabled_at = NULL` (cleared) |
| `ENABLED -> DISABLED` | `UPDATE` | `enabled = false`, `activation_id` retained, `enabled_at` retained, `disabled_at = T` |
| `ENABLED -> ENABLED`, `DISABLED -> DISABLED` | no statement | unchanged |
| `ABSENT -> DISABLED` | no statement | no row created |

Every write therefore sets `disabled_at` and `enabled` consistently in the same
statement, so the constraint is never transiently violated. The upsert of
section 7.5 (`INSERT ... ON CONFLICT (publisher_id, platform) DO UPDATE`) writes
the full enabled tuple in both its insert and its update branch, which is why it
satisfies the invariant for both `ABSENT -> ENABLED` and `DISABLED -> ENABLED`.

**Diesel representation.** The `NOT NULL` columns map to non-optional Rust
fields, which is what makes the invariant visible in the type system rather than
only in the database:

```text
publisher_id  -> Uuid
platform      -> DistributionPlatform
enabled       -> bool
activation_id -> Uuid                 (not Option<Uuid>)
enabled_at    -> Timestamp            (not Option<Timestamp>)
disabled_at   -> Option<Timestamp>    (the only optional lifecycle field)
created_at    -> Timestamp
updated_at    -> Timestamp
```

In `thoth-api/src/schema.rs` these are `Uuid`, the custom
`DistributionPlatform` sql type, `Bool`, `Uuid`, `Timestamptz`,
`Nullable<Timestamptz>`, `Timestamptz`, `Timestamptz`.

**Lifecycle invariants enforced by the database.** The row-level invariant
above, plus the `NOT NULL` declarations and the composite primary key. These are
single-row invariants, expressible with ordinary `CHECK` and `NOT NULL`, and
consistent with the repository's heavy use of named check constraints. They are
required.

**Lifecycle invariants deliberately *not* enforced by the database:**

- the cross-row OAPEN/DOAB linkage (see section 7.2.1);
- `JISC_NBK` non-assignability (see section 7.4).

### 6.2 Composite primary key - explicit note for review

No existing Thoth table uses a composite primary key. This is therefore a
deliberate, specification-level decision, not an accidental deviation. It is
adopted because:

- the approved design states the primary key is `publisher_id` plus `platform`;
- the natural key *is* the identity of the row - there is exactly one assignment
  state per publisher per platform, and re-enabling reuses the same row rather
  than creating a new one;
- it gives the `ON CONFLICT (publisher_id, platform) DO UPDATE` upsert used by
  the lifecycle (section 7.1) a natural arbiter;
- Diesel supports composite primary keys, expressed in `schema.rs` as
  `publisher_distribution_platform (publisher_id, platform)`.

The implementing agent must confirm that Diesel's composite-key support behaves
correctly with the `Queryable` model and the query layer at the exact base, and
must record that verification. If a composite primary key proves incompatible
with the pinned Diesel version's usable API for these queries, that is a
`BLOCKED` stop condition (section 19), not a licence to substitute a surrogate
key silently.

### 6.3 Index

```sql
CREATE INDEX publisher_distribution_platform_enabled_idx
    ON public.publisher_distribution_platform
    USING btree (platform, publisher_id)
    WHERE enabled;
```

This is the enabled-row reverse-lookup index required by the approved design. It
supports `publishersByDistributionPlatform` and
`publisherCountByDistributionPlatform` (both of which filter `platform = $1 AND
enabled`), and its `(platform, publisher_id)` column order matches the lookup
shape and the tie-breaker column. The `WHERE enabled` partial predicate follows
the established repository partial-index convention and keeps the index
proportional to the enabled population.

The composite primary key already provides the `(publisher_id, platform)` index
used by `Publisher.distributionPlatforms`, so no additional index is needed for
the forward lookup. **No further index may be added unless the implementation
demonstrates a concrete need**, recorded with the query shape that requires it.

### 6.4 Rust model and query layer

Add to the new module:

- `PublisherDistributionPlatform` - a Diesel `Queryable`, serde-serializable
  persisted model with fields `publisher_id: Uuid`, `platform:
  DistributionPlatform`, `enabled: bool`, `activation_id: Uuid`,
  `enabled_at: Timestamp`, `disabled_at: Option<Timestamp>`,
  `created_at: Timestamp`, `updated_at: Timestamp`, with
  `#[serde(rename_all = "camelCase")]` per repository convention. Only
  `disabled_at` is optional; `activation_id` and `enabled_at` are non-optional
  because every persisted row exists as the result of an activation
  (section 6.1.1);
- an `Insertable`/upsert type for the assignment write path;
- the domain transition functions of section 7;
- the read functions backing sections 9.2, 9.3 and 9.4.

The reverse lookup and count must live where they can reuse existing publisher
query infrastructure. The recommended placement is a dedicated `impl Publisher`
block (in `thoth-api/src/model/publisher/crud.rs`, alongside the existing
bespoke `Publisher::by_zitadel_ids`) exposing
`Publisher::by_distribution_platform(...)` and
`Publisher::count_by_distribution_platform(...)`, joining
`publisher_distribution_platform` to `publisher`. This requires adding
`joinable!(publisher_distribution_platform -> publisher (publisher_id));` and
adding the table to `allow_tables_to_appear_in_same_query!` in
`thoth-api/src/schema.rs`.

`thoth-api/src/schema.rs` is updated **manually**, in the same bounded PR, under
ADR-0003 Architecture A, preserving the file's existing custom types, aliases,
column ordering, timestamp mappings and formatting, and introducing no unrelated
reformatting. `diesel print-schema` must not be used as the canonical writer.

### 6.5 Prohibited data-model additions

- no audit-history table of any kind (BE-03 owns
  `publisher_service_configuration_history`);
- no distribution-job tables (BE-04);
- no database platform-descriptor table (descriptors are code-owned, ADR-0004
  section 5);
- no cross-domain mapping to `MetricPlatform` (ADR-0002 section 4.5);
- **no production backfill, and no assignment row created merely because
  publishers already exist.** The migration inserts zero rows into
  `publisher_distribution_platform`. Assignment creation for existing publishers
  is `MIG-01` work under separate CRITICAL controls.

---

## 7. Required behaviour

### 7.1 Activation lifecycle

The supported domain write path exposes exactly two logical operations for a
single (publisher, platform) target - **enable** and **disable** - plus their
linked-group forms (section 7.2). Both run inside one transaction (section 7.5).

`T` below denotes the transaction timestamp (`CURRENT_TIMESTAMP`), identical for
every row written by one logical transition.

| Transition | Row write | `activation_id` | `enabled_at` | `disabled_at` | `updated_at` | `enabled` | Idempotent |
|---|---|---|---|---|---|---|---|
| **ABSENT -> ENABLED** | `INSERT` | newly generated `Uuid::new_v4()` | `T` | `NULL` | `T` (`created_at` also `T`) | `true` | n/a (first write); a repeat request becomes ENABLED -> ENABLED |
| **ENABLED -> ENABLED** | **no write at all** | unchanged | unchanged | unchanged | **unchanged** | `true` | **yes** - successful no-op returning the current row; no new activation, no timestamp movement |
| **ENABLED -> DISABLED** | `UPDATE` | **retained unchanged** (it identifies the activation being ended) | **retained unchanged** (it records when that activation began) | `T` | `T` | `false` | n/a; a repeat request becomes DISABLED -> DISABLED |
| **DISABLED -> DISABLED** | **no write at all** | unchanged | unchanged | unchanged | **unchanged** | `false` | **yes** - successful no-op returning the current row |
| **DISABLED -> ENABLED** | `UPDATE` | **newly generated `Uuid::new_v4()`** (a genuine re-enable is a new activation) | `T` | set to `NULL` | `T` | `true` | n/a; a repeat request becomes ENABLED -> ENABLED |
| **ABSENT -> DISABLED** (disable requested for an assignment that does not exist) | **no write at all; no row created** | n/a | n/a | n/a | n/a | n/a | **yes** - successful no-op |

This table and the row-level invariant of section 6.1.1 are two views of one
model and must stay in agreement. In particular:

- **a row is created only by `ABSENT -> ENABLED`.** No other transition inserts,
  and `ABSENT -> DISABLED` creates nothing. That is what makes `activation_id`
  and `enabled_at` `NOT NULL`: there is no persisted "never activated" state;
- **`disabled_at` is the discriminator.** Every write sets `enabled` and
  `disabled_at` together in one statement, so the check constraint of
  section 6.1.1 is satisfied at every commit and never transiently violated;
- **`DISABLED -> ENABLED` must clear `disabled_at` to `NULL`.** Omitting the
  clear is a constraint violation, not a silent inconsistency.

Binding semantics:

- **disabled rows are retained, never deleted**; there is no supported delete
  path for an assignment row in BE-02;
- **an actual disabled-to-enabled transition generates a new `activation_id`**;
- **repeated enabling of an already enabled assignment must not create a new
  activation**, and must not move `updated_at`;
- **linked platforms enabled in one logical operation share one activation
  identity** (section 7.2);
- both no-op transitions return success, not an error, and return the current
  persisted row.

### 7.2 Linked-platform normalization

`OAPEN` and `DOAB` are separate `DistributionPlatform` values that are, for the
initial implementation, a **linked selection** in the group `OAPEN_DOAB`. **The
backend owns the rule.** `thoth-app` will later consume backend linkage metadata
(section 9.1) and must maintain no duplicate rule; no app change belongs in
BE-02.

The linked-group contract:

1. enabling `OAPEN` through the supported domain operation results in both
   `OAPEN` and `DOAB` enabled;
2. enabling `DOAB` through the supported domain operation does the same;
3. disabling either through the supported domain operation results in both
   disabled;
4. a linked enable is performed in **one** transaction;
5. every row written by one linked enable carries the **same** newly generated
   `activation_id`;
6. every row written by one linked transition carries **identical** timestamps,
   guaranteed by using the transaction timestamp `T`;
7. failure updating either row rolls back the **complete** linked operation -
   partial application is impossible;
8. repeated requests do not create unnecessary new activations;
9. **no supported domain write path may create a one-sided enabled OAPEN/DOAB
   state.**

**Group-level evaluation (binding).** A linked operation is evaluated against
the state of the whole group, not each member independently:

- **Linked enable.** If *every* member of the group is already `enabled`, the
  operation is a complete no-op: no write, no new `activation_id`, no timestamp
  movement. Otherwise the operation is a **group activation**: one new
  `activation_id` is generated and *every* member row is written with that
  `activation_id`, `enabled = true`, `enabled_at = T`, `disabled_at = NULL`,
  `updated_at = T` - including any member that was already enabled, whose
  previous `activation_id` is replaced. This is what makes requirement 5
  unconditional, and it repairs any pre-existing one-sided or
  split-activation state (for example one introduced by `MIG-01` or by direct
  SQL) rather than propagating it.
- **Linked disable.** If *no* member of the group is currently `enabled`, the
  operation is a complete no-op. Otherwise every member row that exists is
  written with `enabled = false`, `disabled_at = T`, `updated_at = T`, retaining
  its `activation_id` and `enabled_at`. Members with no row are left absent; no
  row is created by a disable.

Non-linked platforms form a singleton group, so the same rules reduce exactly to
the section 7.1 table.

#### 7.2.1 What "cannot be created" means at the enforcement boundary

The one-sided-enabled prohibition is enforced **through supported backend/domain
behaviour**, in a single transaction, exactly as the approved architecture
requires. Concretely:

- every supported write path for assignment state goes through the group-aware
  transition functions above;
- BE-02 exposes **no** mutation, resolver, CRUD implementation or public API
  that can write an assignment row outside those functions;
- the operation is transactional, so a failure on either row rolls back both;
- the per-publisher lock (section 7.5) serialises concurrent transitions, so
  interleaving cannot produce a one-sided result.

A one-sided enabled state therefore cannot arise from any supported domain
behaviour. It remains physically representable by direct SQL executed outside
the application (for example by a database administrator), which is not a
supported path and is out of BE-02's scope; the group-level enable rule above
additionally *repairs* such a state on the next supported linked enable.

**No database-level cross-row constraint is added.** A cross-row invariant is
not expressible as a PostgreSQL `CHECK`; enforcing it in the database would
require a trigger or an exclusion-constraint mechanism. That would be a new
enforcement subsystem not present in the current architecture, would duplicate
code-owned linkage metadata in the schema (ADR-0004 section 5 makes descriptors
code-owned), and would require a migration whenever a linked group changed. The
approved architecture asks for a normal transactional domain rule, and BE-02
implements exactly that.

#### 7.2.2 Destination is not adapter

`OAPEN` and `DOAB` currently map to **one** operational delivery adapter,
`OAPEN_DOAB_SWORD`. BE-02 must preserve `destination != adapter/feed profile`.
BE-02 creates no delivery job and performs no upload; the shared adapter
identity is internal descriptor metadata only (section 8).

### 7.3 `OCLC_KB` and `EX_LIBRIS_KB`

These are **independent publisher assignments**. They are **not** a linked
selection group. Both currently consume the shared feed profile
`OCLC_KBART_PUBLIC`.

BE-02 must preserve:

- each may be independently enabled and disabled;
- they do **not** share activation identities merely because they share a feed
  profile - each is its own singleton group and gets its own `activation_id`;
- enabling one must not implicitly enable the other;
- enabling both does not create two publisher-level KBART feed states - BE-02
  records **destination assignment state only** and holds no feed state at all,
  so no duplicate feed state is representable;
- neither creates an uploader job (BE-02 creates no jobs at all);
- the descriptor layer represents the shared mechanism identity
  (`OCLC_KBART_PUBLIC`) without collapsing the two destination values.

### 7.4 `JISC_NBK`

`JISC_NBK` is part of the 17-value enum but is initially **inactive**,
**non-assignable** and **job-free**. BE-02 represents and enforces that as
follows:

- it appears as a stable platform option because it is part of the enum:
  `distributionPlatformOptions` returns it like any other value;
- its public option metadata makes the state explicit: `assignable` is `false`
  (section 9.1);
- **the supported domain enable operation rejects it before any write**: the
  transition function checks the descriptor's assignment availability first and
  returns the dedicated error of section 10 without opening or writing in a
  transaction. A linked enable whose group contains a non-assignable member is
  rejected in the same way, as a whole;
- it creates no distribution job and no delivery (BE-02 creates neither for any
  platform);
- **no BE-02 path silently makes it assignable** - assignability is read from
  the single code-owned descriptor and is not parameterisable, configurable at
  runtime, or overridable by input.

`JISC_NBK_MARC_S3` must **not** be implemented. It is future, separately
approved work. The adapter/feed-profile identity may be recorded in the internal
descriptor as an inactive mechanism identity (ADR-0004 section 4.3), which is
metadata only.

Enforcement is **not** placed in a database constraint. Assignability is
code-owned descriptor metadata; a database check would duplicate it and would
require a migration to activate `JISC_NBK` later.

### 7.5 Transaction boundaries and concurrency

- Every state transition (single or linked) executes inside **one**
  `connection.transaction(...)`.
- The transaction first takes a **row lock on the publisher**:
  `SELECT ... FROM publisher WHERE publisher_id = $1 FOR UPDATE`. This
  serialises all concurrent transitions for a given publisher, covers linked
  groups without a second lock ordering problem, and is the lock BE-03 will also
  need for its configuration mutation. It uses only existing repository
  primitives (`thoth-api/AGENTS.md` section 5 explicitly lists row locks) and
  introduces no new locking subsystem.
- Assignment writes use `INSERT ... ON CONFLICT (publisher_id, platform) DO
  UPDATE ...` so that the absent/present cases are handled by one statement and
  a concurrent first-arrival cannot produce a duplicate-key failure.
- Reads used to decide the transition are performed **inside** the transaction,
  after the lock is taken, so the decision is made against a stable snapshot.

**Concurrent contradictory or duplicate transitions.** Two writers targeting the
same publisher serialise on the publisher row lock. The second writer's decision
is made against the first writer's committed state, so:

- two concurrent *enable* requests for the same absent assignment produce
  exactly one activation: the first inserts and generates an `activation_id`;
  the second observes `ENABLED` and is a no-op;
- concurrent *enable* and *disable* requests both succeed, and the final state
  is that of the later-committing transaction (last writer wins) - no lost
  update, no torn linked pair, no orphan activation;
- two concurrent *linked* enables produce exactly one group activation for the
  same reason.

Transitions targeting **different** publishers do not contend.

BE-03 later owns the protected service-configuration mutation and the
optimistic-concurrency (`expectedUpdatedAt`) public API. BE-02 provides the
sound domain persistence and normalization foundation without absorbing that
public mutation surface.

---

## 8. Code-owned descriptors

Descriptors are **code-owned metadata, not database rows**. No descriptor table
may be created, and no second destination registry may be introduced.

**Every one of the 17 `DistributionPlatform` variants must have exactly one
descriptor**, and exhaustiveness must be guaranteed **at compile time with no
runtime fallback**.

**Required implementation shape** (the exact signatures may follow repository
conventions, but the properties are binding):

```rust
pub struct DistributionPlatformDescriptor {
    pub platform: DistributionPlatform,
    pub display_label: &'static str,
    pub linked_group: Option<DistributionPlatformGroup>,
    pub back_catalogue_behaviour: BackCatalogueBehaviour,
    pub assignment_availability: AssignmentAvailability,
    pub mechanism_readiness: MechanismReadiness,
    pub adapter_profile: DistributionAdapterProfile,
}

impl DistributionPlatform {
    pub const ALL: [DistributionPlatform; 17] = [ /* canonical order */ ];
    pub const fn descriptor(self) -> &'static DistributionPlatformDescriptor {
        match self { /* one arm per variant; no wildcard */ }
    }
}
```

Required properties:

- `descriptor` is implemented as an **exhaustive `match self` with one arm per
  variant and no `_` arm**, so adding an eighteenth variant fails to compile.
  An equivalent static construction that provides the same compile-time
  exhaustiveness guarantee is acceptable; a `HashMap`, a lazily initialised
  registry, a lookup returning `Option`, or any construction with a fallback arm
  is not;
- descriptors are `&'static` data with no allocation;
- there is exactly one canonical descriptor definition; no consumer may
  duplicate any descriptor field;
- `DistributionPlatform::ALL` is the canonical ADR-0004 section 4.1 order and is
  the single source of iteration order for `distributionPlatformOptions`.

**Descriptor vocabularies** - all closed Rust enums with no fallback variant:

```text
BackCatalogueBehaviour:      AutomaticPush | PullFeed | Manual
DistributionPlatformGroup:   OapenDoab                       (GraphQL: OAPEN_DOAB)
AssignmentAvailability:      Assignable | NonAssignable
MechanismReadiness:          Active | Inactive
DistributionAdapterProfile:  IA_API, OAPEN_DOAB_SWORD, SCIENCEOPEN_FTP,
                             CUL_SWORD, CROSSREF_DOI_DEPOSIT, FIGSHARE_API,
                             ZENODO_API, MUSE_FTP, JSTOR_FTP, EBSCO_HOST_SFTP,
                             PROQUEST_EBOOK_CENTRAL_FTP, GOOGLE_PLAY_GCS,
                             BKCI_FTP, OCLC_KBART_PUBLIC, JISC_NBK_MARC_S3
```

The back-catalogue behaviour vocabulary is **exactly** `AutomaticPush`,
`PullFeed`, `Manual` - no fourth value, no `Unknown`, no `None`.

`DistributionAdapterProfile` has 15 values for 17 platforms:
`OAPEN_DOAB_SWORD` is shared by `OAPEN` and `DOAB`, and `OCLC_KBART_PUBLIC` is
shared by `OCLC_KB` and `EX_LIBRIS_KB`. This is precisely how the descriptor
represents shared mechanism identity without collapsing destination values. It
is **internal**: it is not exposed through GraphQL in BE-02 (section 9.1).

**Binding descriptor content** (ADR-0004 sections 4.1 and 5):

| Platform | Display label | Behaviour | Linked group | Adapter profile | Readiness | Availability |
|---|---|---|---|---|---|---|
| `INTERNET_ARCHIVE` | Internet Archive | AutomaticPush | none | `IA_API` | Active | Assignable |
| `OAPEN` | OAPEN | AutomaticPush | `OAPEN_DOAB` | `OAPEN_DOAB_SWORD` | Active | Assignable |
| `DOAB` | DOAB | AutomaticPush | `OAPEN_DOAB` | `OAPEN_DOAB_SWORD` | Active | Assignable |
| `SCIENCE_OPEN` | ScienceOpen | Manual | none | `SCIENCEOPEN_FTP` | Active | Assignable |
| `CAMBRIDGE_UNIVERSITY_LIBRARY` | Cambridge University Library | AutomaticPush | none | `CUL_SWORD` | Active | Assignable |
| `CROSSREF` | Crossref | AutomaticPush | none | `CROSSREF_DOI_DEPOSIT` | Active | Assignable |
| `FIGSHARE` | Figshare | AutomaticPush | none | `FIGSHARE_API` | Active | Assignable |
| `ZENODO` | Zenodo | AutomaticPush | none | `ZENODO_API` | Active | Assignable |
| `PROJECT_MUSE` | Project MUSE | AutomaticPush | none | `MUSE_FTP` | Active | Assignable |
| `JSTOR` | JSTOR | AutomaticPush | none | `JSTOR_FTP` | Active | Assignable |
| `EBSCO_HOST` | EBSCOHost | AutomaticPush | none | `EBSCO_HOST_SFTP` | Active | Assignable |
| `PROQUEST_EBOOK_CENTRAL` | ProQuest Ebook Central | AutomaticPush | none | `PROQUEST_EBOOK_CENTRAL_FTP` | Active | Assignable |
| `GOOGLE_PLAY` | Google Play Books | AutomaticPush | none | `GOOGLE_PLAY_GCS` | Active | Assignable |
| `BKCI` | Book Citation Index | AutomaticPush | none | `BKCI_FTP` | Active | Assignable |
| `OCLC_KB` | OCLC Knowledge Base | PullFeed | none | `OCLC_KBART_PUBLIC` | Active | Assignable |
| `EX_LIBRIS_KB` | Ex Libris Knowledge Base | PullFeed | none | `OCLC_KBART_PUBLIC` | Active | Assignable |
| `JISC_NBK` | Jisc NBK | AutomaticPush | none | `JISC_NBK_MARC_S3` | **Inactive** | **NonAssignable** |

Display labels are stable API-adjacent strings; they must match this table
exactly and are covered by tests.

**`AutomaticPush` creates nothing in BE-02.** A descriptor value of
`AutomaticPush` is a statement about future behaviour. It must not cause BE-02
to create a durable job, a target, a queue entry, a scheduled action or any
other side effect. Durable distribution jobs are BE-04.

Update and withdrawal expectations from ADR-0004 section 5 are recorded
architecture. BE-02 implements no update or withdrawal behaviour. Crossref
remains the only initially verified automatic-push update path; all other
automatic updates remain disabled until separately approved. There are no
automatic withdrawals.

---

## 9. Public GraphQL contract

All four surfaces are **public**, consistent with the current architecture in
which the entire `QueryRoot` read API is anonymously accessible (section 2.2 and
section 11). All use the repository's existing offset/limit pagination style; no
connection, edge, cursor or `pageInfo` type may be introduced.

### 9.1 `distributionPlatformOptions`

```graphql
distributionPlatformOptions: [DistributionPlatformOption!]!
```

Backend-owned descriptor metadata, derived entirely from the code-owned
descriptors. It takes **no arguments**, performs **no database access**, and
returns **all 17 options** including the non-assignable `JISC_NBK`.

```graphql
type DistributionPlatformOption {
  platform: DistributionPlatform!
  displayLabel: String!
  linkedGroup: DistributionPlatformGroup
  backCatalogueBehaviour: BackCatalogueBehaviour!
  assignable: Boolean!
}
```

Implemented with `#[derive(juniper::GraphQLObject)]` (precedent:
`FileUploadResponse`), with a description on the type and on every field.

Field semantics:

- `platform` - the stable platform identity;
- `displayLabel` - the stable display label from the descriptor table;
- `linkedGroup` - **nullable**; non-null exactly for `OAPEN` and `DOAB`, both
  returning `OAPEN_DOAB`. This is the linked-platform relationship the app
  requires, supplied by the backend so the app maintains no independent rule;
- `backCatalogueBehaviour` - `AUTOMATIC_PUSH` | `PULL_FEED` | `MANUAL`. Exposed
  because it is approved descriptor content (ADR-0002 section 4.1, ADR-0004
  section 5), contains no credential or secret, and is needed by later
  consumers to explain to users why a destination does or does not receive
  automatic delivery;
- `assignable` - `false` exactly for `JISC_NBK`, `true` for the other 16. This
  makes the unavailable/non-assignable state explicit in public metadata.

**Not exposed** (binding):

- the adapter/feed-profile identity (`DistributionAdapterProfile`) - internal
  operational configuration; exposing it would publish a second
  business-platform identity alongside the destination enum and invite consumers
  to key behaviour on the mechanism rather than the destination;
- mechanism readiness - internal operational state; `assignable` already carries
  everything a consumer must act on;
- any credential, endpoint, host, bucket, account or secret operational
  configuration - never;
- update or withdrawal expectations - not needed by any BE-02 consumer.

Ordering is the canonical `DistributionPlatform::ALL` order (ADR-0004 section
4.1) and is deterministic and stable.

### 9.2 `Publisher.distributionPlatforms`

```graphql
type Publisher {
  # ... existing fields unchanged ...
  distributionPlatforms: [PublisherDistributionPlatformAssignment!]!
}

type PublisherDistributionPlatformAssignment {
  platform: DistributionPlatform!
  enabledAt: Timestamp!
}
```

Added to the existing explicit `#[juniper::graphql_object] impl Publisher` in
`thoth-api/src/graphql/model.rs`, following the `Publisher.imprints` /
`Publisher.contacts` precedent.

Binding requirements:

- returns **enabled assignments only** (`WHERE publisher_id = $1 AND enabled`);
- **disabled historical rows must never be exposed** as active publisher
  platforms;
- deterministic ordering: `ORDER BY platform ASC`, which is PostgreSQL enum
  declaration order and therefore the canonical inventory order. No tie is
  possible because `(publisher_id, platform)` is the primary key;
- returns an empty list, successfully, when the publisher has no enabled
  assignment;
- `enabledAt` is non-null: the `enabled_at NOT NULL` declaration of section 6.1
  guarantees it for every persisted row;
- `activationId`, `disabledAt`, `enabled`, `createdAt` and `updatedAt` are
  **not** exposed. `activationId` is an internal correlation identity for BE-04
  job deduplication; `disabledAt` would expose historical state this field
  explicitly must not show;
- **no package data may become public through this field** - the assignment type
  contains no package or capability field, and the `Publisher` object still
  exposes no `subscriptionPackage` or `capabilities`;
- the field takes no `limit`/`offset` arguments: the result is bounded above by
  17 rows by construction.

#### 9.2.1 N+1 access - binding prohibition and open architecture escalation

`thoth-api/AGENTS.md` section 6 requires that new lists and reports "avoid N+1
access" and "use set-based SQL or batched loaders". That requirement is retained
in full and is **not** weakened, narrowed or waived by this specification.

**Binding prohibition.** The BE-02 implementation must not perform one
assignment query per parent publisher when `Publisher.distributionPlatforms` is
selected on a publisher **list**. The exposure is concrete and is not confined
to BE-02's own root fields:

```graphql
publishers(limit: 100) { publisherId distributionPlatforms { platform } }
publishersByDistributionPlatform(platform: OAPEN, limit: 100) {
  publisherId distributionPlatforms { platform }
}
```

Both operations place the new child field under a list parent. The first uses
the **pre-existing** `publishers` root query, so no change to BE-02's own new
root fields can remove the exposure.

**Live repository finding (verified at the specification preflight base).** The
repository has **no** batching or set-based child-field mechanism to reuse:

- no DataLoader, `dataloader` dependency, `batch_load` or equivalent anywhere in
  `thoth-api` or `thoth-api-server`;
- no use of Juniper's `look_ahead` / `LookAheadSelection`;
- `crate::graphql::model::Context` holds only `db`, `user`, `s3_client` and
  `cloudfront_client`, with no request-scoped cache or accumulator;
- every existing child field on an object type - `Publisher.imprints`,
  `Publisher.contacts`, `Work.contributions` and the rest - resolves by calling
  `X::all(...)` or `X::from_id(...)` once per parent. The established repository
  pattern for child fields *is* per-parent querying.

**Consequence.** Satisfying section 6 for this field cannot be done by following
an existing repository pattern, because no compliant pattern exists. Each
available route is a decision above this task's authority:

| Option | What it requires | Why it is not this task's decision |
|---|---|---|
| A - request-scoped batching in the GraphQL `Context` | adding shared per-request state to `Context`, which is constructed in `thoth-api-server` and used by every query resolver and every mutation; settling cache coherence against mutations in the same operation | cross-cutting GraphQL architecture with repository-wide precedent for all 56 existing per-parent child resolvers |
| B - a DataLoader crate | a new workspace dependency, and Juniper 0.16 sync execution (`execute_sync`) would constrain the integration | a dependency change, which this specification may not introduce |
| C - documented bounded exception to section 6 for this field | accepting per-parent access on the basis that the child result is hard-bounded at 17 rows per publisher by the composite primary key, with an explicit maximum `limit` on publisher lists | a deliberate exception to a standing repository control, owned by the control's owner |
| D - defer `Publisher.distributionPlatforms` out of BE-02 | removing a field the approved design and ADR-0002 section 4.4 both require | contradicts approved architecture |

**Status: escalated to the CTO. This is a blocking gate.** BE-02 implementation
must not be authorized until the CTO settles which option applies. The exact
decision required is recorded in section 19 as a named stop condition. This
specification deliberately does not choose, because every option either changes
shared GraphQL architecture, adds a dependency, waives a standing control, or
removes approved public API.

**What is already settled regardless of the outcome**, and what the
implementation must satisfy under any option:

1. `Publisher.distributionPlatforms` remains in BE-02's public contract
   (sections 9.2 and 12) unless option D is explicitly chosen by the CTO;
2. enabled-only filtering (section 9.2) is preserved;
3. deterministic child ordering by `platform` (section 9.2) is preserved;
4. the child result stays bounded at 17 rows per publisher by the composite
   primary key;
5. the chosen mechanism must be proven by the query-count evidence required in
   section 18.7b - a narrative claim is not acceptable;
6. whichever option is chosen, no per-parent database access may remain
   unmeasured and undocumented in the implementation report.

The behaviour to be demonstrated for each parent shape, once the mechanism is
settled:

| Parent shape | Required behaviour |
|---|---|
| a single `publisher(publisherId:)` | one assignment query is acceptable under every option |
| `publishers(limit: n)` | must not issue `n` assignment queries under options A or B; under option C the bounded per-parent access must be explicit, measured and within the agreed `limit` ceiling |
| `publishersByDistributionPlatform(platform:, limit: n)` | same requirement as `publishers` |
| the same publisher referenced more than once in one operation (aliased fields) | must not multiply database access; under options A or B one load serves all references |

### 9.3 `publishersByDistributionPlatform`

```graphql
publishersByDistributionPlatform(
  platform: DistributionPlatform!
  limit: Int = 100
  offset: Int = 0
  order: PublisherOrderBy = { field: PUBLISHER_NAME, direction: ASC }
): [Publisher!]!
```

Binding requirements:

- accepts the stable GraphQL platform enum (not a string). An invalid enum value
  fails Juniper input coercion before the resolver runs;
- returns publishers with an **enabled** assignment for that platform only:
  `INNER JOIN publisher_distribution_platform ON publisher_id WHERE platform =
  $1 AND enabled`;
- uses the repository's existing offset/limit pagination style, argument names,
  defaults and descriptions;
- **paginates deterministically.** Ordering is the requested `PublisherOrderBy`
  field and direction, **always** followed by `publisher_id ASC` as tie-breaker,
  using the existing `apply_directional_order!` secondary-expression form. The
  tie-breaker is mandatory and is not optional or configurable. (Note that the
  existing `Publisher::all` has no tie-breaker; BE-02 must not copy that
  defect);
- returns an **empty successful page** when no publisher has an enabled
  assignment for the requested platform - never an error, never a null;
- **must not broaden scope on failure**: a database error propagates as an
  error; it never degrades to "all publishers" or to an unfiltered list;
- **must not infer assignments** from locations, `LocationPlatform` values,
  packages, dissemination configuration, workflow configuration or legacy
  environment variables. The only source is
  `publisher_distribution_platform.enabled`;
- returns at most one row per publisher (guaranteed by the primary key).

### 9.4 `publisherCountByDistributionPlatform`

```graphql
publisherCountByDistributionPlatform(platform: DistributionPlatform!): Int!
```

Binding requirements:

- counts **exactly the same enabled-assignment population** as section 9.3,
  using the same filter predicate;
- returns `0` when there are no enabled assignments;
- **never uses a fallback source** and never falls back to a total publisher
  count;
- returns `i32` using the established `Crud::count` conversion idiom;
- semantics for `JISC_NBK` are the ordinary semantics with **no special
  casing**: because no supported path can enable `JISC_NBK`, the count is `0`
  and the reverse lookup is empty. The query must not hard-code `0`, must not
  error, and must not filter on assignability - it reports the actual persisted
  enabled population, which is what makes it agree with section 9.3 by
  construction.

Count and reverse-lookup agreement must be proven by test (section 18).

---

## 10. Failure semantics and fail-closed behaviour

Two binding principles carry through BE-02 and into every later consumer:

```text
configuration failure fails closed
an empty assignment set does not broaden processing
```

Even though dissemination cutover is later work, **no BE-02 query or domain
helper may encode fallback semantics that downstream code could interpret as
"all publishers"**. There is no "if no assignments, return everything" path, no
`unwrap_or(all)`, no default-open branch and no legacy-environment fallback
inside Thoth.

| Condition | Required behaviour |
|---|---|
| Database error in any query or transition | propagate as `ThothError::DatabaseError` -> GraphQL error. Never return a partial, unfiltered or broadened result |
| Invalid state transition | not representable: the six transitions of section 7.1 are total over `{ABSENT, ENABLED, DISABLED} x {enable, disable}`. Any state that violates the row check constraint is rejected by the database |
| Attempt to enable `JISC_NBK` (directly or as part of a group) | rejected **before any write**, with a dedicated stable error variant added to `thoth-errors`, materially `ThothError::DistributionPlatformNotAssignable(String)` carrying the stable platform code. No transaction is opened; no row is created or modified |
| Linked OAPEN/DOAB transaction failure on either row | the complete transaction rolls back; neither row is written; the error propagates. No one-sided state results |
| Empty reverse lookup | successful empty list |
| No assignments for a publisher | successful empty list from `Publisher.distributionPlatforms` |
| Invalid GraphQL enum input | Juniper input-coercion failure before the resolver runs; no query executes; no default platform is substituted |
| Stale or disabled rows | excluded from every public read by the `enabled` predicate; retained in storage; never treated as active |
| Unknown database enum label reaching Rust | Diesel deserialization error -> `ThothError::DatabaseError`. No fallback variant exists to absorb it |

---

## 11. Authorization and public exposure analysis

**Why these platform queries are public.** The approved design states plainly:
"Platform assignments are public. Package values are not." ADR-0002 section 4.4
names `distributionPlatformOptions` as a public surface. Publisher participation
in a distribution destination is already externally observable (records appear
in OAPEN, DOAB, JSTOR, Internet Archive and the public OCLC KBART feed), and
`EXP-01` must later publish an OCLC KBART publisher index derived from exactly
this data. Making it public is consistent with the current repository
architecture, in which the whole `QueryRoot` read API is anonymously accessible.

**Which callers may use them.** All callers, including anonymous ones. BE-02
introduces **no new authorization surface**, **no new role**, **no new policy**
and **no change to `thoth-api/src/policy.rs`**.

**What must remain absent, proven by negative tests:**

- no package or capability data is reachable through any BE-02 type or field -
  `DistributionPlatformOption`, `PublisherDistributionPlatformAssignment` and
  the `Publisher` object all lack `subscriptionPackage` and `capabilities`;
- no protected BE-03 service-configuration surface is introduced;
- no adapter/feed-profile identity, endpoint, credential or operational secret
  is reachable;
- no mutation of any kind is added;
- `internal` `activationId` and `disabledAt` are not reachable.

**Anonymous query tests are required.** Because these surfaces are intentionally
public, the tests must positively assert anonymous reachability of the four
public surfaces *and* assert the absence of package/capability/protected fields,
so that a later accidental exposure fails a test. BE-02 must **not** introduce
BE-03 owner/superuser authorization, and must **not** add a protected
configuration mutation.

BE-02 must not log tokens, secrets, credentials, sensitive object URLs, personal
source data or unbounded input.

---

## 12. Compatibility

### 12.1 Exact public GraphQL contract inventory

This inventory is **binding** and must agree item for item with section 9. Any
disagreement between section 9 and this table is a defect.

**New root query fields - exactly 3:**

| # | Field | Return type |
|---:|---|---|
| 1 | `distributionPlatformOptions` | `[DistributionPlatformOption!]!` (section 9.1) |
| 2 | `publishersByDistributionPlatform` | `[Publisher!]!` (section 9.3) |
| 3 | `publisherCountByDistributionPlatform` | `Int!` (section 9.4) |

**New fields on existing types - exactly 1:**

| # | Type | Field | Return type |
|---:|---|---|---|
| 1 | `Publisher` | `distributionPlatforms` | `[PublisherDistributionPlatformAssignment!]!` (section 9.2) |

Total new public surfaces: **4** (3 root fields + 1 field on `Publisher`).

**New object types - exactly 2:**

| # | Type | Defined in |
|---:|---|---|
| 1 | `DistributionPlatformOption` | section 9.1 |
| 2 | `PublisherDistributionPlatformAssignment` | section 9.2 |

**New GraphQL enums - exactly 3:**

| # | Enum | Values |
|---:|---|---|
| 1 | `DistributionPlatform` | the 17 stable codes of section 5.0 |
| 2 | `DistributionPlatformGroup` | `OAPEN_DOAB` |
| 3 | `BackCatalogueBehaviour` | `AUTOMATIC_PUSH`, `PULL_FEED`, `MANUAL` |

**Internal Rust enums that are deliberately NOT GraphQL enums - exactly 3.**
These exist in the descriptor layer (section 8) and must not appear in the
generated SDL. An internal Rust enum is not a public GraphQL enum merely because
it exists:

| # | Rust enum | Why it is not exposed |
|---:|---|---|
| 1 | `AssignmentAvailability` | surfaced publicly only as the derived `assignable: Boolean!` |
| 2 | `MechanismReadiness` | internal operational state (section 9.1) |
| 3 | `DistributionAdapterProfile` | internal operational configuration; exposing it would publish a second business-platform identity (section 9.1) |

**New GraphQL inputs, mutations, scalars or interfaces: none.**
`publishersByDistributionPlatform` reuses the existing `PublisherOrderBy` input
and `Direction` enum; the existing `Timestamp` scalar is reused unchanged.

### 12.2 Compatibility assessment

The implementation report must assess and record:

- **GraphQL contract**: purely additive, exactly as inventoried in section 12.1 -
  3 new root query fields, 1 new `Publisher` field, 2 new object types, 3 new
  GraphQL enums, 0 new inputs, 0 mutations, 0 new scalars. No existing type,
  field, input, argument, enum value or nullability is changed or removed;
- **generated schema**: `thoth-client/assets/schema.graphql` is regenerated at
  build time and is untracked; record the observed diff, confirm it is purely
  additive, and confirm it contains **exactly** the section 12.1 inventory - in
  particular that `AssignmentAvailability`, `MechanismReadiness` and
  `DistributionAdapterProfile` are absent from the generated SDL;
- **internal Rust client** (`thoth-client`): `assets/queries.graphql` is the
  tracked query source and requires no change because BE-02 adds no query the
  internal client consumes; confirm `cargo build -p thoth-client` and
  `cargo test -p thoth-export-server` still pass, and that no generated enum
  conversion is hidden behind a wildcard;
- **`thoth-app` generated client**: no change required or made in this task;
- **database**: additive only - one new type, one new table, one new index, one
  new trigger; no existing table, column, constraint, index or trigger is
  altered; no row is created, changed or deleted;
- **`thoth-errors`**: one added variant. Adding a variant to a non-exhaustive
  consumer match could break compilation; confirm the workspace compiles and
  that no existing error mapping is changed;
- **dissemination, export, OAI and Metrics**: no behaviour change; BE-02 adds no
  consumer of the new data;
- **deployment**: no ordering constraint beyond applying the migration before
  serving the new queries, which is the ordinary deployment sequence.

Any unexpected generated-client or public-contract change requires review before
continuing.

---

## 13. Data and migration requirements

Migration required: **YES**. BE-02 is a schema-bearing HIGH-risk task and must
follow ADR-0003 Architecture A **atomically**: the same bounded implementation
PR must include the migration `up.sql` and `down.sql`,
`thoth-api/src/schema.rs`, the affected models, the affected query/GraphQL code,
the focused tests, the migration evidence and the compatibility evidence.

The implementation must **not** introduce `diesel_cli`, `diesel print-schema` as
canonical writer, a root `diesel.toml`, a schema synchronizer or any structural
schema-generation subsystem. Create the migration with `make migration`; apply
and revert with `cargo run migrate` and `cargo run migrate --revert`.

### 13.1 Empty-database evidence

On a **disposable** PostgreSQL database only:

1. apply the complete migration chain with `cargo run migrate`;
2. verify the resulting BE-02 schema: the `distribution_platform` type exists
   with exactly the 17 values in the declared order; the
   `publisher_distribution_platform` table exists with the exact columns, types,
   nullability and defaults; the composite primary key, the foreign key with
   `ON DELETE CASCADE`, the named check constraint, the partial index and the
   `set_updated_at` trigger all exist;
3. revert the complete chain with `cargo run migrate --revert`, verifying that
   BE-02's `down.sql` executes successfully and the database returns to the
   expected pre-migration state;
4. reapply the complete chain with `cargo run migrate`;
5. verify the resulting schema again.

`--revert` is a **full-history** operation. Its success is *migration
reversibility evidence only*; it is not evidence of an isolated BE-02-only
revert and does not authorize operational rollback (section 15).

### 13.2 Representative populated-database evidence

Construct a **disposable** pre-BE-02 database containing representative existing
data:

- multiple publishers with varied names, shortnames, URLs and null/non-null
  optional fields;
- existing BE-01 publisher package state (`subscription_package`, all `OASIS`);
- publisher history rows;
- representative unrelated rows (imprints, works, publications, locations) so
  that unrelated-data preservation is genuinely testable.

Record publisher IDs, row counts and representative field values. Then apply
BE-02 as the pending migration with `cargo run migrate`, and verify:

- every existing publisher is preserved: same IDs, same count, same field
  values;
- existing package values and publisher history are preserved and readable;
- **`publisher_distribution_platform` contains zero rows** - no assignment is
  synthesized for any existing publisher;
- no distribution job or any other record is created (none exist to create);
- no unrelated row in any table changes;
- the schema, constraint and index results are exactly as specified.

**Do not run the destructive full-history `cargo run migrate --revert` against
these representative populated fixtures** merely to claim operational rollback.
This path validates forward-migration preservation; section 13.1 separately
validates chain reversibility. This is BE-01's accepted evidence precedent.

### 13.3 Locking and downtime

**The migration does take a lock on the populated `publisher` table.** It is
not lock-free, and it must not be described as such. The foreign key of section
6.1 references `publisher`, and establishing a foreign key locks the *referenced*
table as well as the table carrying the constraint.

The deployed and tested PostgreSQL major version is **17** (`postgres:17` in both
`docker-compose.yml` and `.github/workflows/run_migrations.yml`).

#### 13.3.1 Locks taken, by operation

| DDL operation | Lock on the new `publisher_distribution_platform` | Lock on the populated `publisher` |
|---|---|---|
| `CREATE TYPE public.distribution_platform` | n/a | none |
| creating the table and establishing the foreign key | `SHARE ROW EXCLUSIVE` on the table carrying the constraint | **`SHARE ROW EXCLUSIVE` on `publisher`** |
| `CREATE INDEX` (partial, on the new table) | `SHARE` on the new table | none |
| `CREATE TRIGGER set_updated_at` (on the new table) | `SHARE ROW EXCLUSIVE` on the new table | none |
| adding the composite primary key and the check constraint | `ACCESS EXCLUSIVE` on the new table | none |

The referenced-table lock is stated by the PostgreSQL 17 `ALTER TABLE`
documentation:

> "Although most forms of `ADD table_constraint` require an `ACCESS EXCLUSIVE`
> lock, `ADD FOREIGN KEY` requires only a `SHARE ROW EXCLUSIVE` lock. Note that
> `ADD FOREIGN KEY` also acquires a `SHARE ROW EXCLUSIVE` lock on the referenced
> table, in addition to the lock on the table on which the constraint is
> declared."

#### 13.3.2 What that lock does and does not block

Per the PostgreSQL 17 explicit-locking documentation, `SHARE ROW EXCLUSIVE`
conflicts with `ROW EXCLUSIVE`, `SHARE UPDATE EXCLUSIVE`, `SHARE`,
`SHARE ROW EXCLUSIVE`, `EXCLUSIVE` and `ACCESS EXCLUSIVE`, and does **not**
conflict with `ACCESS SHARE` or `ROW SHARE`. For the `publisher` table, while
the lock is held:

| Concurrent operation on `publisher` | Lock it needs | Blocked? |
|---|---|---|
| `SELECT` | `ACCESS SHARE` | **No** |
| `SELECT ... FOR UPDATE` / `FOR SHARE` | `ROW SHARE` | **No** |
| `INSERT` / `UPDATE` / `DELETE` | `ROW EXCLUSIVE` | **Yes** |
| `VACUUM`, `CREATE INDEX CONCURRENTLY`, `ANALYZE` | `SHARE UPDATE EXCLUSIVE` | **Yes** |
| `CREATE INDEX` | `SHARE` | **Yes** |
| another `ADD FOREIGN KEY` referencing `publisher` | `SHARE ROW EXCLUSIVE` | **Yes** (self-conflicting) |

So publisher **reads remain available throughout**, and publisher **writes are
blocked for the duration the lock is held**.

#### 13.3.3 Conservative operational assessment

- **No scan of `publisher` is required.** Foreign-key validation checks child
  rows against the parent, and the child table is created empty in the same
  migration, so there are no rows to validate. The lock is therefore expected to
  be held only briefly.
- **The dominant risk is lock acquisition, not lock duration.** The request must
  wait for any in-flight conflicting transaction on `publisher` to finish, and
  while it waits, subsequent write requests queue behind it. A single
  long-running transaction touching `publisher` can therefore stall publisher
  writes for as long as that transaction runs, even though the migration's own
  work is trivial.
- **`NOT VALID` is not proposed.** It exists to avoid scanning a large child
  table; here the child table is empty, so it would add a second migration step
  and a lasting unvalidated constraint for no benefit. It also would not remove
  the referenced-table lock at constraint-creation time.
- **This is not a destructive or long-locking migration**, and it must not be
  described as one. It is an additive migration that briefly blocks writes to one
  populated table.

#### 13.3.4 Required evidence and prohibitions

The future implementation must:

1. **reassess the actual generated DDL**, not copy this section. The lock profile
   depends on the exact statements emitted - in particular whether the foreign
   key is declared inline in `CREATE TABLE ... REFERENCES` or added by a separate
   `ALTER TABLE ... ADD CONSTRAINT` - and on the PostgreSQL version in use;
2. **verify the referenced-table lock empirically** on a disposable database, for
   example by inspecting `pg_locks` joined to `pg_class` from a second session
   while the migration transaction is open, and record the observed lock modes
   on both `publisher` and `publisher_distribution_platform`;
3. record the deployed PostgreSQL major version the assessment applies to;
4. state explicitly that publisher **reads** are unaffected and publisher
   **writes** are blocked while the lock is held;
5. assess whether a `lock_timeout` should be set for the migration session, and
   record the conclusion either way;
6. **make no production-duration claim.** No production evidence exists, no
   production database access is authorized, and no timing has been measured in
   any production-like environment;
7. state that **production migration execution remains separately gated** by
   CG-13 and the current release controls, and has been neither tested nor
   authorized by this specification.

Any claim that the migration "takes no lock on any populated table" is
incorrect and must not appear in the implementation report, the changelog or the
pull-request description.

---

## 14. Rollout

These are five separate events. Each requires its own trigger or authorization,
and **none of them causes any later one**:

```text
1. repository merge                    <- all the implementation PR does
2. environment deployment              <- separately authorized and separately performed
3. environment migration execution     <- separately authorized; production gated by CG-13
4. assignment creation / backfill      <- MIG-01 or BE-03; separately authorized (CRITICAL for production)
5. runtime consumer activation/cutover <- DIS-01/DIS-02; separately authorized
```

#### 14.1 What the repository merge actually guarantees

A merge changes `develop`. It deploys nothing, runs no migration, and touches no
environment. After the implementation pull request merges, and **only** in the
repository:

- the migration, `thoth-api/src/schema.rs`, the models, the domain logic and the
  four public GraphQL surfaces are present in `develop`;
- the change is additive and backwards compatible: no existing type, field,
  input, enum value, table, column, constraint or index is altered or removed;
- no consumer of the new data exists in the codebase;
- no dissemination, export, OAI or Metrics code path changes.

No environment has changed, no schema has been applied anywhere, and no row
exists anywhere, **because a merge does none of those things**.

#### 14.2 Behaviour in an environment, once separately authorized steps occur

The following is **conditional**, and describes an environment only after events
2 and 3 above have been separately authorized and performed.

In an environment where the BE-02 application version has been deployed **and**
the BE-02 migration has been applied, but before any assignment rows have been
created (event 4):

- `publisher_distribution_platform` exists and is empty;
- `distributionPlatformOptions` returns the 17 code-owned descriptors, which
  require no database rows;
- `Publisher.distributionPlatforms` returns an empty list for every publisher;
- `publishersByDistributionPlatform` returns an empty page for every platform;
- `publisherCountByDistributionPlatform` returns `0` for every platform;
- no distribution job exists, because BE-02 implements none;
- dissemination continues to use its existing configuration source, unchanged.

In an environment where events 2 and 3 have **not** occurred, none of the above
applies: the table does not exist and the queries are not served, because the
deployed application version does not contain them.

The empty-result behaviour is a property of an empty table, not of a merge, and
it must not be stated as a post-merge fact.

Feature flag/configuration: none required, because no feature consumes the
foundation.

Staging/preview validation: disposable-database and exact-head CI evidence. No
production migration is performed or authorized by BE-02.

Pilot: not applicable to inactive, empty storage.

Activation approval: **CG-13 and the current release controls continue to govern
production migration and release execution.** Production migration, deployment,
release and activation each require separate explicit authorization, with
verified runtime and migration ownership, the exact command and approver, a
rollback decision and backup/restore evidence where required.

The future implementation PR itself requires **explicit CTO merge
authorization**, bound to the exact independently reviewed head, because BE-02
is HIGH risk. That merge authorization does not replace, imply or grant any
production authorization.

Observation period: defined by the later release/operational task, not BE-02.

---

## 15. Rollback

### 15.1 Migration reversibility evidence

The committed `down.sql` must execute successfully as part of the disposable
full-chain apply/revert/reapply test in section 13.1. That is **migration
reversibility evidence only**. It is not the approved operational rollback
strategy after BE-02 merges, and it does not authorize destructive post-merge
use of the down migration.

### 15.2 Approved operational rollback

Operational rollback is a property of an **environment**, so it applies only
where the BE-02 version has been deployed and its migration applied (section
14.2). A repository merge needs no operational rollback; withdrawing it is an
ordinary revert of the pull request.

Following the BE-01 precedent, operational rollback in such an environment
**retains the additive foundation**:

- retain the `distribution_platform` PostgreSQL type;
- retain the `publisher_distribution_platform` table, its constraints, index and
  trigger, and every stored row;
- retain the `DistributionPlatform` enum, the descriptors and the domain types
  required to preserve the persisted schema contract;
- do not activate any consumer;
- correct defects by **reviewed forward repair**, not by executing the down
  migration;
- leave publisher, package and all other configuration unchanged.

Because BE-02 introduces no consuming feature path, its immediate operational
response is: do not activate a consumer; leave the additive foundation inactive;
forward-repair defects under a separately reviewed release.

Feature disable/kill switch: no BE-02 feature path exists to disable. If a
public query itself needed to be withdrawn, that is a reviewed forward change
removing the resolver, not a schema rollback.

External side effects: none. BE-02 performs no external call, deposit or upload.
If any is observed, stop and investigate.

### 15.3 Destructive rollback after production assignments exist

**Dropping populated assignment data is not the normal production rollback.**
Once production assignments exist (created by `MIG-01` or by BE-03), any
destructive rollback:

- must be **separately authorized** by the CTO;
- must be **data-preserving** (export or retain the assignment rows before any
  destructive step);
- requires its own bounded task, migration plan, independent review and release
  controls appropriate to the resulting risk.

Removing the assignment foundation entirely would change the approved
architecture and requires an approved ADR amendment or replacement first. The
implementing agent must stop rather than improvise a destructive rollback, and
return:

```text
BLOCKED - ROLLBACK REQUIRES ADR CHANGE
```

---

## 16. Observability and operations

Required logs: **none new**. BE-02 adds no background process, no external call
and no operation warranting new logging. Existing GraphQL query logging covers
the new queries. No token, secret, credential, sensitive URL or personal source
data may be logged.

Required metrics/alerts: **none** for BE-02, because no feature consumes the
foundation and the tables are empty. `OPS-01` owns monitoring for activated
components.

Operational runbook changes: **none** in BE-02. CG-13 continues to require a
separately verified production migration, deployment, rollback, restore and
approver gate before any production action.

---

## 17. Acceptance criteria

- [ ] `DistributionPlatform` contains exactly the 17 ADR-0004 values, with the
      exact stable codes, in the canonical order.
- [ ] Every enum variant has exactly one code-owned descriptor, and descriptor
      exhaustiveness is guaranteed at compile time with no fallback arm.
- [ ] No fallback, `OTHER`, catch-all, `Default` or wildcard-granting value or
      arm exists anywhere in the BE-02 surface.
- [ ] `DistributionPlatform` and `MetricPlatform` remain separate domains with
      no conversion and no mapping.
- [ ] The PostgreSQL type is `public.distribution_platform` with exactly the 17
      uppercase labels in the canonical declaration order.
- [ ] `publisher_distribution_platform` exists with the exact columns, types,
      nullability, defaults, composite primary key `(publisher_id, platform)`,
      `ON DELETE CASCADE` foreign key, named lifecycle check constraint,
      partial enabled index and `set_updated_at` trigger of section 6.
- [ ] The migration creates zero assignment rows on a populated database.
- [ ] `enabled`, `activation_id` and `enabled_at` are `NOT NULL`; `disabled_at`
      is the only nullable lifecycle column; `enabled` carries no database
      default.
- [ ] The row-level invariant of section 6.1.1 holds: `enabled` is true exactly
      when `disabled_at IS NULL`, and the database rejects an enabled row with a
      `disabled_at`, a disabled row without one, a row without an
      `activation_id` and a row without an `enabled_at`.
- [ ] No never-activated persisted row can exist, because only
      `ABSENT -> ENABLED` inserts and `ABSENT -> DISABLED` creates no row.
- [ ] `publisher_distribution_platform` follows the approved lifecycle model of
      section 7.1 for all six transitions, and every write sets `enabled` and
      `disabled_at` consistently in one statement.
- [ ] Disabled assignment rows are retained, never deleted.
- [ ] A genuine disabled-to-enabled transition creates a new `activation_id`.
- [ ] Idempotent same-state operations perform no write, create no activation
      and do not move `updated_at`.
- [ ] A one-sided enabled OAPEN/DOAB state cannot be created through any
      supported domain behaviour.
- [ ] OAPEN/DOAB linked transitions are atomic and share one activation
      identity and identical timestamps.
- [ ] `OCLC_KB` and `EX_LIBRIS_KB` remain independent assignments with
      independent activation identities.
- [ ] `OCLC_KB` and `EX_LIBRIS_KB` create no duplicate feed state and no job.
- [ ] `JISC_NBK` is visible as an option with `assignable: false` but cannot be
      assigned through any supported path.
- [ ] `Publisher.distributionPlatforms` returns enabled assignments only, in
      deterministic order, exposing no package data.
- [ ] `publishersByDistributionPlatform` returns enabled publishers only.
- [ ] Reverse-lookup pagination is deterministic, with a mandatory
      `publisher_id` tie-breaker, and page boundaries are stable.
- [ ] `publisherCountByDistributionPlatform` and the reverse lookup agree on the
      same enabled population.
- [ ] Empty results are successful empty results, never errors and never
      broadened results.
- [ ] No BE-02 query or helper encodes an "all publishers" or legacy-environment
      fallback.
- [ ] Platform assignments are public without exposing package, capability,
      adapter/feed-profile, credential or protected BE-03 data.
- [ ] Anonymous access to the four public surfaces is proven, and the absence of
      package/capability/protected fields is proven by negative tests.
- [ ] The public GraphQL contract is exactly the section 12.1 inventory: 3 new
      root query fields, 1 new `Publisher` field, 2 new object types, 3 new
      GraphQL enums, 0 inputs, 0 mutations, 0 new scalars; section 9 and section
      12.1 agree item for item.
- [ ] `AssignmentAvailability`, `MechanismReadiness` and
      `DistributionAdapterProfile` are absent from the generated SDL.
- [ ] No per-parent N+1 assignment query is performed for
      `Publisher.distributionPlatforms` on publisher lists, under the mechanism
      settled by the section 9.2.1 architecture decision, and the query count is
      proven by measurement rather than narrative.
- [ ] The section 9.2.1 N+1 architecture decision has been settled by the CTO
      before implementation is authorized.
- [ ] The migration works on an empty database (apply/revert/reapply) and on a
      representative populated database (forward preservation).
- [ ] `thoth-api/src/schema.rs` is updated manually and atomically in the same
      PR under ADR-0003 Architecture A, with no unrelated reformatting.
- [ ] No Diesel CLI, `diesel.toml`, `print-schema` canonical generation or
      schema synchronizer is introduced.
- [ ] No distribution job, target, attempt, worker role, lease or claim is
      implemented or created.
- [ ] No production platform assignment is backfilled.
- [ ] No BE-03 protected query, mutation or audit history is introduced.
- [ ] No dissemination, export, OAI or Metrics behaviour changes.
- [ ] No production migration, deployment, release, activation or cutover is
      performed or authorized.
- [ ] The DDL lock assessment is recorded from the actual generated DDL and
      states the `SHARE ROW EXCLUSIVE` lock taken on the populated `publisher`
      table by the foreign key, that publisher reads are unaffected and
      publisher writes are blocked while it is held, the PostgreSQL major
      version assessed, and the `lock_timeout` conclusion - with **no**
      unevidenced production-duration claim and no assertion that the migration
      locks no populated table.
- [ ] `CHANGELOG.md` is updated under `## [Unreleased]` with the PR reference.
- [ ] The BE-02 implementation report is complete and exact.
- [ ] Required focused and full workspace checks pass, with exact commands and
      results recorded.
- [ ] Fresh independent exact-head review returns `APPROVED`, and explicit CTO
      merge authorization is bound to that exact head.

---

## 18. Required tests

Generic "add tests" is not acceptable. The following concrete tests are
required. Unit tests live with the model; database-backed tests use the existing
disposable-database harness (`thoth-api/src/model/tests.rs`, `TEST_DATABASE_URL`);
GraphQL tests use `execute_graphql` in `thoth-api/src/graphql/tests.rs`.

### 18.1 Enum and conversion

- the enum has exactly 17 variants, and `DistributionPlatform::ALL` contains
  each exactly once in the canonical order;
- every variant round-trips through PostgreSQL (`DbEnum`) storage and retrieval;
- every variant's `db_rename` label matches the exact stable code;
- serde serialization and deserialization produce and accept exactly the stable
  codes;
- `Display` and `FromStr` produce and accept exactly the stable codes;
- the GraphQL enum exposes exactly the 17 stable value names;
- no `OTHER`, fallback, default or alias value exists, and
  `DistributionPlatform` implements no `Default`;
- an invalid `FromStr` input fails; an invalid serde value fails; an unknown
  database label produces an error rather than a fallback;
- an invalid GraphQL enum input fails coercion and executes no query.

### 18.2 Descriptors

- every enum variant resolves to exactly one descriptor, and descriptor coverage
  is exhaustive over `DistributionPlatform::ALL`;
- every descriptor's `platform` field equals the variant it is returned for;
- display labels match the section 8 table exactly for all 17 values;
- `OAPEN` and `DOAB` both advertise linked group `OapenDoab`, and no other
  platform advertises any linked group;
- `OCLC_KB` and `EX_LIBRIS_KB` share the `OCLC_KBART_PUBLIC` adapter profile and
  are **not** in a linked group;
- `OAPEN` and `DOAB` share the `OAPEN_DOAB_SWORD` adapter profile;
- `JISC_NBK` is `Inactive`, `NonAssignable` and job-free; all other 16 are
  `Active` and `Assignable`;
- the `BackCatalogueBehaviour` vocabulary is exactly the three approved values,
  and each platform's behaviour matches the section 8 table;
- descriptor construction allocates nothing at call time.

### 18.3 Assignment lifecycle (database-backed)

- ABSENT -> ENABLED inserts one row with a new `activation_id`, `enabled = true`,
  `enabled_at` set, `disabled_at` null;
- ENABLED -> ENABLED is idempotent: no new `activation_id`, unchanged
  `enabled_at`, **unchanged `updated_at`**, still exactly one row;
- ENABLED -> DISABLED sets `enabled = false` and `disabled_at`, retains
  `activation_id` and `enabled_at`, and **retains the row**;
- DISABLED -> DISABLED is idempotent with no timestamp movement;
- DISABLED -> ENABLED sets a **different** `activation_id` from the previous
  activation, sets a new `enabled_at`, and clears `disabled_at`;
- ABSENT -> DISABLED creates no row and succeeds;
- timestamps follow the section 7.1 state machine exactly.

#### 18.3a Row-level invariant (database-backed, direct SQL)

These assert the section 6.1.1 invariant at the database boundary, bypassing the
domain layer, so that a future defect in application code cannot persist an
illegal state:

- **an enabled persisted row cannot carry a `disabled_at`** - a direct
  `INSERT`/`UPDATE` setting `enabled = true` with a non-null `disabled_at` is
  rejected by `publisher_distribution_platform_enabled_state_check`;
- **a disabled persisted row must carry a `disabled_at`** - a direct write
  setting `enabled = false` with `disabled_at IS NULL` is rejected by the same
  constraint;
- **every persisted row has a non-null activation identity** - a direct insert
  with `activation_id` omitted or `NULL` is rejected by `NOT NULL`;
- **every persisted row has a non-null `enabled_at`** - a direct insert with
  `enabled_at` omitted or `NULL` is rejected by `NOT NULL`;
- **`enabled` has no database default** - a direct insert omitting `enabled` is
  rejected by `NOT NULL` rather than silently producing a disabled row;
- across the full supported transition sequence
  (`ABSENT -> ENABLED -> DISABLED -> ENABLED -> DISABLED`), every intermediate
  committed state satisfies the invariant, and `disabled_at` is `NULL` exactly
  when `enabled` is true;
- `ABSENT -> DISABLED` creates no row, so no never-activated row can exist;
- a query for rows violating `enabled = (disabled_at IS NULL)` returns zero rows
  after every test in section 18.3.

### 18.4 Linked OAPEN/DOAB (database-backed)

- enabling `OAPEN` enables both `OAPEN` and `DOAB`;
- enabling `DOAB` enables both;
- disabling either disables both;
- one linked enable writes the **same** `activation_id` to both rows and
  **identical** `enabled_at` values;
- a linked enable of an already fully enabled group creates no new activation
  and moves no timestamp;
- a linked enable of a partially enabled group produces one new shared
  activation across both rows (state repair);
- the linked write is atomic: an injected failure on the second row rolls back
  the first, leaving the pre-transaction state intact;
- after any sequence of supported linked operations, no one-sided enabled state
  exists;
- repeated and concurrent linked transitions follow the specified idempotency
  and locking behaviour: two concurrent linked enables of the same absent group
  produce exactly one shared activation across both rows.

### 18.5 `OCLC_KB` / `EX_LIBRIS_KB`

- each can be enabled and disabled independently;
- enabling one leaves the other absent/unchanged - no implied linked state;
- both enabled independently hold **different** `activation_id` values;
- both descriptors report the same feed profile while remaining separate
  destinations and separate assignment rows;
- no job or feed state is created by either (no such table exists).

### 18.6 `JISC_NBK`

- `distributionPlatformOptions` returns `JISC_NBK`;
- its option reports `assignable: false`;
- a supported domain enable attempt fails closed with the dedicated error and
  **writes nothing** (row count unchanged, no row created);
- a linked/group enable that includes `JISC_NBK` fails as a whole;
- no assignment for `JISC_NBK` ever becomes enabled;
- `publisherCountByDistributionPlatform(JISC_NBK)` returns `0` and the reverse
  lookup returns an empty page, through the ordinary predicate and not by
  special casing.

### 18.7 GraphQL

- `distributionPlatformOptions` returns exactly 17 options in canonical order;
- all options use the stable platform identities and correct display labels;
- linked metadata is correct: exactly `OAPEN` and `DOAB` return `OAPEN_DOAB`;
- `JISC_NBK` metadata is explicit (`assignable: false`);
- `Publisher.distributionPlatforms` returns enabled assignments only, in
  deterministic `platform` order;
- disabled rows are excluded from `Publisher.distributionPlatforms`;
- no package or capability data appears in any BE-02 response;
- `publishersByDistributionPlatform` returns enabled publishers only, and
  excludes publishers whose assignment is disabled or absent;
- pagination is deterministic across pages with duplicate sort keys: publishers
  sharing a `publisher_name` paginate stably by the `publisher_id` tie-breaker,
  and page boundaries neither duplicate nor skip a publisher;
- an empty reverse lookup is a successful empty list;
- `publisherCountByDistributionPlatform` agrees with the reverse-lookup
  population across a fixture with a mix of enabled, disabled and absent
  assignments;
- anonymous (unauthenticated) execution of all four public surfaces succeeds, as
  intended;
- no protected BE-03 surface, mutation or configuration field is introduced -
  asserted by inspecting the schema for the absence of
  `publisherServiceConfiguration`, `replacePublisherServiceConfiguration`,
  `subscriptionPackage` and `capabilities` on the public surface.

#### 18.7a Contract-inventory agreement (section 12.1)

Assert the exact inventory against the generated schema, so that section 9 and
section 12.1 cannot silently drift apart:

- the generated SDL contains exactly the **3** new root query fields named in
  section 12.1, and no other new root field;
- the generated SDL contains exactly **1** new field on `Publisher`
  (`distributionPlatforms`);
- the generated SDL contains exactly the **2** new object types named in section
  12.1;
- the generated SDL contains exactly the **3** new GraphQL enums named in
  section 12.1, with exactly their listed values (17, 1 and 3 respectively);
- the generated SDL contains **no** `AssignmentAvailability`,
  `MechanismReadiness` or `DistributionAdapterProfile` type;
- BE-02 adds no GraphQL input, mutation, scalar or interface;
- no pre-existing type, field, argument, enum value or nullability is removed or
  changed.

#### 18.7b N+1 query-count evidence

Required once the section 9.2.1 architecture decision is settled. A narrative
claim that N+1 is avoided is **not** acceptable evidence.

- a list operation selecting `Publisher.distributionPlatforms` under
  `publishers(limit: n)` is executed against a fixture of `n` publishers, and
  the number of assignment queries issued is **measured**, not asserted by
  inspection - for example through a counting/instrumented connection, a
  `pg_stat_statements` delta, or another method the repository can support;
- the same measurement is made for
  `publishersByDistributionPlatform(platform:, limit: n)`;
- the measured query count matches the mechanism chosen under section 9.2.1, and
  the implementation report records the exact figure for at least two distinct
  values of `n`, showing whether it scales with `n`;
- the same publisher referenced twice in one operation through field aliases does
  not multiply database access under options A or B;
- enabled-only filtering and deterministic `platform` ordering of the child field
  remain correct under the chosen mechanism, including when the parent is a list;
- if the CTO selects option C (bounded exception), the test instead asserts the
  agreed maximum `limit` ceiling is enforced, and the report states the measured
  per-parent query count explicitly rather than omitting it.

### 18.8 Database and index

- composite primary key uniqueness: a second row for the same
  `(publisher_id, platform)` is rejected;
- **foreign-key definition is correct**: the constraint references
  `public.publisher(publisher_id)` with `ON DELETE CASCADE`, verified against the
  catalogue (`pg_constraint`) rather than only against the migration text;
- foreign-key behaviour: deleting a publisher cascades and removes its
  assignment rows; inserting an assignment for a non-existent publisher is
  rejected;
- the `NOT NULL` declarations on `enabled`, `activation_id` and `enabled_at`, the
  nullability of `disabled_at`, and the absence of a database default on
  `enabled` are verified against the catalogue;
- the partial enabled index exists with the expected definition and, where
  practical, supports the intended lookup shape (verify with `EXPLAIN` on a
  fixture large enough for the planner to prefer it, or record why that is not
  practical);
- migration on an empty database (section 13.1);
- migration on a representative populated database (section 13.2), preserving
  existing publishers, package values and history and creating zero assignment
  rows;
- migration revert and reapply (section 13.1);
- **the referenced-table lock is observed empirically** (section 13.3.4): with
  the migration transaction open, a second session inspects `pg_locks` joined to
  `pg_class` and the observed lock modes on both `publisher` and
  `publisher_distribution_platform` are recorded. The implementation report
  states the deployed PostgreSQL major version the assessment applies to, states
  that publisher reads are unaffected and publisher writes are blocked while the
  lock is held, and **claims no production lock duration**.

### 18.9 Regression

- the full workspace checks required by repository instructions pass;
- generated GraphQL schema and internal client remain compatible, and
  `thoth-client` / `thoth-export-server` build and test cleanly;
- existing publisher and package behaviour is unchanged: publisher CRUD,
  publisher history, `publishers`, `publisher`, `publisherCount` and existing
  ordering behaviour are unaffected;
- no dissemination, export, OAI or Metrics runtime behaviour changes.

### 18.10 Required commands

At minimum:

```bash
cargo test -p thoth-api --features backend
cargo test --workspace
cargo check --workspace
cargo clippy --all --all-targets --all-features -- -D warnings
cargo fmt --all -- --check
cargo run migrate
cargo run migrate --revert
git diff --check
```

Record exact commands and concise exact outcomes. Do not report only "tests
passed". Use local or disposable PostgreSQL and Redis only; never point any
command at production or a shared service.

### 18.11 Performance

No runtime throughput or latency target applies, because BE-02 activates no
consuming feature and the assignment table holds no rows until assignments are
separately created (section 14.2).

Three items of performance-related evidence are nonetheless mandatory:

- the DDL lock assessment of section 13.3, including the `SHARE ROW EXCLUSIVE`
  lock on the populated `publisher` table;
- the reverse-lookup index justification of section 6.3;
- the N+1 query-count evidence of section 18.7b, once the section 9.2.1
  architecture decision is settled.

---

## 19. Stop conditions

### 19.1 Open blocking gate carried by this specification

One architecture decision is **escalated and unresolved**, and it gates
implementation authorization:

```text
BLOCKED - N+1 CONTROL REQUIRES ARCHITECTURE DECISION
```

**Exact decision required from the CTO.** `thoth-api/AGENTS.md` section 6
requires new lists and reports to avoid N+1 access and to use set-based SQL or
batched loaders. The repository provides no batching or set-based child-field
mechanism to reuse: there is no DataLoader, no `look_ahead` usage, no
request-scoped state on the GraphQL `Context`, and all 56 existing child-field
resolvers query once per parent. Adding `Publisher.distributionPlatforms` -
which the approved design and ADR-0002 section 4.4 both require - therefore
cannot satisfy section 6 by following any existing repository pattern, and the
exposure also arises through the pre-existing `publishers` root query, so no
change confined to BE-02's own root fields removes it.

The CTO must choose one of the options set out in section 9.2.1:

- **A** - authorize a request-scoped batching mechanism on the GraphQL
  `Context` (cross-cutting; sets repository-wide precedent; likely its own
  bounded prerequisite task);
- **B** - authorize a DataLoader dependency (a new workspace dependency, with
  Juniper 0.16 sync execution constraining the integration);
- **C** - grant a documented, bounded exception to `thoth-api/AGENTS.md`
  section 6 for this field, justified by the hard 17-row-per-publisher bound,
  with an agreed maximum `limit` on publisher lists;
- **D** - defer `Publisher.distributionPlatforms` out of BE-02 (contradicts the
  approved design and ADR-0002 section 4.4).

Until that decision is recorded, BE-02 implementation must not be authorized.
The implementing agent must stop and report the label above rather than choosing
an option, weakening the section 6 requirement, or shipping an unmeasured
per-parent query. Nothing else in this specification is affected by the choice:
sections 9.2, 9.3, 9.4 and 12.1 hold under every option except D, which would
require a specification amendment.

### 19.2 Stop conditions for the implementing agent

The implementing agent must stop and report `BLOCKED` if:

- the section 9.2.1 / 19.1 N+1 architecture decision has not been recorded by
  the CTO before implementation begins;
- ADR-0004 is not repository-authoritative at the implementation base, or the
  live inventory differs from the 17-value inventory of section 5.0;
- ADR-0005 is not authoritative on the then-current `develop`;
- ADR-0003 Architecture A would need to change, or implementation would require
  `diesel_cli`, a root `diesel.toml`, `diesel print-schema` as canonical writer,
  or a schema synchronizer;
- a low-level choice would actually change approved architecture rather than
  follow repository convention;
- BE-02 cannot prevent supported one-sided OAPEN/DOAB state without adding an
  architectural mechanism not already approved (for example a trigger subsystem
  or an exclusion-constraint framework);
- a composite primary key proves incompatible with the pinned Diesel version's
  usable query API for the specified queries;
- the public/private API boundary conflicts with current approved architecture;
- BE-02 would need to absorb BE-03, BE-04, MIG-01, DIS-01, EXP-01, OAI-01 or
  production work;
- production credentials, production data access or a shared service would be
  required;
- the migration proves unexpectedly destructive or long-locking on a populated
  table;
- representative populated migration evidence cannot be produced;
- a required test path cannot run against a disposable database;
- the current repository state materially contradicts this specification's
  premise, or the exact implementation base moves after branching;
- a cross-programme decision involving Metrics or another programme is required;
- an inventory or architecture change appears necessary - escalate to the CTO
  rather than changing it in BE-02;
- implementation cannot be completed without an unrelated change;
- removal of the assignment foundation would be proposed as rollback - use
  `BLOCKED - ROLLBACK REQUIRES ADR CHANGE`.

When blocked, identify the **exact CTO decision required**. Do not resolve
architectural conflicts by inference.

---

## 20. Expected implementation report

Use `docs/engineering/ai-delivery/implementation-report-template.md`, written to
`docs/engineering/ai-delivery/implementation-reports/BE-02-implementation-report.md`.

It must include:

- the exact base and head commits;
- the actual changed files;
- the migration SQL and the DDL/lock operational assessment, **reassessed from
  the actual generated DDL** (section 13.3.4), including the observed
  `pg_locks` evidence for the referenced `publisher` table, the PostgreSQL major
  version assessed, the reads-unaffected/writes-blocked statement, and the
  `lock_timeout` conclusion - with no production-duration claim;
- the recorded CTO decision on the section 9.2.1 N+1 architecture gate, the
  mechanism implemented as a result, and the measured query-count evidence of
  section 18.7b for at least two values of `n`;
- the section 12.1 contract-inventory agreement evidence, including confirmation
  that the three internal Rust enums are absent from the generated SDL;
- empty-database and representative populated-database evidence;
- the `thoth-api/src/schema.rs` baseline and the exact resulting diff, confirmed
  to contain only the approved type, table, joinable and
  `allow_tables_to_appear_in_same_query!` additions with no unrelated
  reformatting;
- descriptor exhaustiveness evidence;
- lifecycle, linked-normalization and concurrency evidence;
- GraphQL contract, authorization and non-exposure evidence;
- generated schema and internal client assessment;
- exact commands and outcomes;
- CI status;
- the post-merge repository state (section 14.1), stated separately from any
  environment behaviour, with no claim that an environment changed because the
  pull request merged;
- migration reversibility evidence and the retained-foundation operational
  rollback;
- known limitations and deferred work;
- explicit confirmation that no production action occurred, no credential was
  used, no workflow was dispatched and issue #765 was not modified.

Independent review must focus explicitly on: the 17-value inventory fidelity;
descriptor exhaustiveness with no fallback; the six lifecycle transitions and
their idempotency; the section 6.1.1 row invariant as actually implemented in
SQL and in the Diesel types; linked atomicity and shared activation identity;
`JISC_NBK` fail-closed enforcement; the measured N+1 evidence against the
recorded CTO decision; the referenced-table lock assessment; deterministic
pagination and count/lookup agreement; the section 12.1 contract inventory;
the absence of any broadening fallback; public/non-exposure boundaries; and the
atomic ADR-0003 schema update.

---

## 21. Recommended execution

```text
Implementation model:      Codex / GPT-5-class or a later approved
                           implementation-capable model
Implementation reasoning:  HIGH or maximum practical
Independent reviewer:      an independent agent/model that did not implement the
                           task; cross-model review preferred
Review reasoning:          HIGH or maximum practical
CTO merge authorization:   required, bound to the exact independently reviewed
                           PR head
Production authorization:  separate and absent
```

The implementing agent may provide a self-assessment but may not approve its own
work or merge its own PR. The agent that authored this specification does not
approve this specification or the future implementation.

---

## 22. Branch and integration plan

```text
Task branch:                 feature/publisher-services/be-02
Base branch:                 develop
PR target:                   develop
Programme integration branch: None
Workflow:                    STANDARD
Branch deletion after merge: YES
Final programme PR required: NO
Final release path:          develop -> master
```

**The exact implementation base SHA is deliberately not pinned by this
specification, and pinning one here would not constitute authorization.**

Required sequence before any implementation branch exists:

1. independent review of this complete specification;
2. explicit CTO approval of this specification;
3. **the CTO decision on the open N+1 architecture gate of sections 9.2.1 and
   19.1**, recorded before implementation is authorized;
4. a fresh read-only preflight at implementation time;
5. fresh verification of the then-current exact `develop` head;
6. explicit CTO authorization to implement BE-02 **bound to that exact
   `develop` SHA**;
7. only then, creation of `feature/publisher-services/be-02` from that verified
   base, with the exact base recorded before any change.

One bounded task, one implementation branch, one implementation PR. There is no
long-lived `feature/publisher-services` integration branch.

**No implementation branch may be created by the specification task.**

---

## 23. Lifecycle boundary

These are different events. **None of them grants any later one**, and no event
in this list is recorded in this file:

| # | Event | Where its evidence lives |
|---:|---|---|
| 1 | specification authored | this file's content |
| 2 | specification independently reviewed | GitHub pull-request review record |
| 3 | specification approved by the CTO | GitHub pull-request record |
| 4 | specification merged, making it repository-authoritative | GitHub merge event and merge commit; the authority condition at the head of this file |
| 5 | implementation explicitly authorized, bound to a freshly verified exact `develop` SHA | CTO authorization record (section 22) |
| 6 | implementation completed | the implementation pull request |
| 7 | implementation independently reviewed at its exact head | GitHub review record on that pull request |
| 8 | CTO authorization to merge the HIGH-risk implementation, bound to that same exact head | GitHub record on that pull request |
| 9 | implementation merged | GitHub merge event and merge commit |
| 10 | environment deployment, environment migration execution, assignment creation/backfill, runtime consumer activation | separate authorizations; see section 14 |

Events 1-4 concern **this specification**. Events 5-9 concern the **future
implementation**. Event 10 concerns **environments**, and is reached by none of
the preceding events.

Two distinctions matter and are frequently conflated:

- **Repository authority is not CTO approval.** This specification becomes
  repository-authoritative when its exact content is reachable from `develop`
  (event 4). That is a property of the Git history. It is not, and does not
  imply, event 3.
- **Approval of this specification is not authorization to implement.** Event 3
  approves the *content* of the specification. Event 5 is a separate, explicit
  CTO decision bound to a freshly verified `develop` SHA, without which
  `feature/publisher-services/be-02` must not exist.

Authoring this specification grants none of the later events.

Under ADR-0005, live review, authorization and merge evidence is the **GitHub
pull-request record**. No repository commit is created to copy review
identifiers, approval identifiers, merge-authorization identifiers, merge SHAs,
timestamps or completion state into Markdown. No approval-state-only commit is
created. No post-merge pull request is opened merely to record that a pull
request merged. A material post-merge correction still requires its own bounded
task and pull request.

---

## 24. Approval

```text
Specification approval authority:  CTO
Specification approval evidence:   GitHub pull-request record
Implementation authorization:      separate and absent
```

This section records the **approval authority and its effect**, which are
durable. It does not record whether approval has occurred, which is a live
GitHub fact (section 23, event 3).

What CTO approval of this specification does:

- it settles the content of this specification as the approved bounded scope,
  invariants, acceptance criteria and required evidence for BE-02.

What CTO approval of this specification does **not** do:

- it does not authorize BE-02 runtime implementation. That requires the separate
  explicit CTO authorization of section 22 step 6, bound to a freshly verified
  exact `develop` SHA, and without it `feature/publisher-services/be-02` must
  not be created;
- it does not authorize the future HIGH-risk implementation pull request to
  merge. That pull request requires its own exact-head independent cross-model
  review and its own explicit CTO merge authorization;
- it does not authorize environment deployment, environment migration execution,
  release, assignment creation or backfill, runtime activation, cutover,
  credential access or any related-repository change.

Repository authority is separate again: this record is repository-authoritative
when this exact content is reachable from `develop`, which is a property of the
Git history rather than an approval.

Independent review decisions, CTO approval, CTO merge authorization and the
merge itself are terminal GitHub evidence under ADR-0005 and must not be copied
back into this file. No approval-state-only commit may be created against this
specification.

**BE-02 runtime implementation is NOT AUTHORIZED by this specification.**
