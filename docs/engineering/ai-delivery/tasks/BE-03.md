# BE-03 - Protected service configuration

Status: DRAFT
Programme: Publisher Services and Distribution Configuration
Repository: `thoth-pub/thoth`
Workflow: STANDARD
Base branch: `develop`
PR target: `develop`
Programme integration branch: None
Risk: HIGH
Owner: CTO
Approved by: specification approval authority is the CTO; approval evidence is
the GitHub pull-request record
Dependencies: P0-01 (CLOSED); ADR-0001 (APPROVED, merged); ADR-0002 (APPROVED,
merged); ADR-0003 / `THOTH-DB-CTRL-02` (APPROVED, merged); ADR-0004 / ADR-01
(APPROVED AND REPOSITORY-AUTHORITATIVE, merged through PR #783); ADR-0005
(APPROVED, merged); ADR-0007 (APPROVED AND REPOSITORY-AUTHORITATIVE, merged
through PR #800); `THOTH-GQL-DATALOADER-01` (merged through PR #802); BE-01
(CLOSED, merged through PR #779); BE-02 (CLOSED, merged through PR #805)
Target branch name: `feature/publisher-services/be-03`
Specification branch: `feature/publisher-services/be-03-spec`
Master programme issue:
[#765](https://github.com/thoth-pub/thoth/issues/765)

Authority condition: this record is repository-authoritative when this exact
content is reachable from the repository's authoritative integration branch
(`develop`). Live review, authorization and merge evidence is the GitHub
pull-request record and is not copied here.

Implementation authorization: **separate and absent.** This document specifies
BE-03; it does not authorize it. The branch `feature/publisher-services/be-03`
must not exist until the CTO separately and explicitly authorizes
implementation against a freshly verified exact `develop` head. Section 23
defines the lifecycle boundary and section 24 the approval boundary.

Specification authoring base: `bcb6ce3081abb14467798b372fcc3e6af9da1c6a`
(the merge commit of BE-02 implementation PR
[#805](https://github.com/thoth-pub/thoth/pull/805)), plus the BE-02 post-merge
control correction delivered by
[`BE-02-CLOSEOUT-01`](BE-02-CLOSEOUT-01.md). That SHA is a
specification-authoring record only. It is not an implementation base and
confers no implementation authorization.

---

## 1. Objective

Add the protected publisher service-configuration surface: one owner-and-superuser
readable representation of a publisher's desired service configuration, one
superuser-only staff report over that configuration, and one superuser-only
mutation that replaces it atomically under optimistic concurrency control, with
durable audit history recording the actor, the source and the before/after
canonical state of every committed semantic change.

BE-03 owns **desired configuration only**. It creates no distribution job, no
job target and no job attempt; it performs no dissemination; it activates no
destination; and it executes no production migration.

### 1.1 Risk rationale

BE-03 is HIGH risk because it introduces, in one task:

1. protected exposure of the commercial subscription package, which BE-01
   deliberately left with no API surface at all;
2. an authorization decision distinguishing owner, other-publisher,
   non-superuser and superuser callers;
3. the programme's first superuser-only mutation over publisher service
   configuration;
4. persistent audit history containing actor identity;
5. a multi-table desired-state change that must be atomic;
6. optimistic concurrency with a new canonical version token;
7. linked-platform normalization that must reuse, not re-implement, BE-02
   domain invariants;
8. a public GraphQL SDL change that adds protected operations and makes the
   BE-01 `ThothPackage` enum SDL-reachable for the first time;
9. downstream contract authority for APP-01 and APP-02;
10. the transaction seam that BE-04 will later extend to create durable jobs
    atomically with a configuration change.

### 1.2 Required HIGH-risk controls

1. an approved bounded specification (this document) that is
   repository-authoritative on `develop` before implementation;
2. separate explicit CTO implementation authorization bound to a freshly
   verified exact `develop` head;
3. one bounded branch and one bounded pull request;
4. fresh independent exact-head review by an agent/model that did not
   implement the task;
5. explicit CTO merge authorization bound to that exact implementation head;
6. deployment, environment migration execution, production migration,
   assignment creation/backfill and distribution activation remain separately
   gated after merge.

## 2. Background and authority

Authoritative sources:

- the approved Publisher Services and Distribution Configuration Technical
  Design and Implementation Plan, Drive revision `3`, indexed in
  [`docs/engineering/design-references.md`](../../design-references.md);
- [ADR-0001](../../decisions/ADR-0001-publisher-package-capability-model.md) -
  package capability model; package changes never alter distribution
  assignments;
- [ADR-0002](../../decisions/ADR-0002-platform-domain-boundaries.md) -
  distribution and metrics platform domains stay separate;
- [ADR-0003](../../decisions/ADR-0003-repository-authoritative-schema-contract.md) -
  `thoth-api/src/schema.rs` is the manually maintained, repository-authoritative
  Diesel schema contract, changed atomically with the migration in one bounded
  pull request;
- [ADR-0004](../../decisions/ADR-0004-distribution-platform-inventory.md) and
  the [final inventory](../../../publisher-services/platform-inventory.md);
- [ADR-0005](../../decisions/ADR-0005-terminal-merge-evidence.md) - terminal
  merge evidence and durable status prose;
- [ADR-0007](../../decisions/ADR-0007-conventional-request-scoped-graphql-dataloader.md) -
  request-local non-cached DataLoader, loader-first adoption, `try_load` only,
  total batch functions, set-based SQL behind the blocking Diesel boundary;
- [BE-01](BE-01.md) and [BE-02](BE-02.md) and their implementation reports;
- the merged code: `thoth-api/src/policy.rs`,
  `thoth-api/src/model/publisher/`,
  `thoth-api/src/model/publisher_distribution_platform/`,
  `thoth-api/src/graphql/`, `thoth-api/src/schema.rs`,
  `thoth-errors/src/lib.rs`.

### 2.1 Verified current behaviour

Re-derived from the merged code at the authoring base:

1. **Authorization** is ZITADEL project roles on `IntrospectedUser`.
   `policy.rs` defines `Role::{Superuser, PublisherAdmin, PublisherUser,
   WorkLifecycle, CdnWrite}`. `Superuser` is an unscoped project role;
   the others are scoped to a ZITADEL organisation id.
   `PolicyContext::require_superuser`, `require_authentication`,
   `require_publisher_for` and `require_role_for_publisher` already exist.
   `require_role_for_publisher` short-circuits for superusers and otherwise
   resolves the publisher's `zitadel_id` through the `PublisherId` trait.
2. **Publisher-to-account linkage** is `publisher.zitadel_id`
   (`Nullable<Text>`). `PublisherId::zitadel_id` returns
   `ThothError::Unauthorised` when it is `NULL`, so an unlinked publisher
   fails closed. An account may legitimately hold scoped roles for more than
   one organisation, so "own publisher" is a per-publisher role check, never a
   single-publisher identity. **There is no local account table and none is
   required.**
3. **Actor identity** is `IntrospectedUser.user_id`, a `String`, exposed as
   `PolicyContext::user_id()`. The existing `publisher_history` table already
   records exactly this as `user_id text NOT NULL`, alongside `data jsonb` and
   a timestamp, written inside the same transaction as the entity update by
   the shared `Crud::update` macro. BE-03 follows that established convention.
4. **Package** is `publisher.subscription_package` of PostgreSQL type
   `thoth_package`, Rust `ThothPackage` (BE-01). It has **no** GraphQL field on
   the public `Publisher` type, and `ThothPackage` is therefore **not currently
   reachable in the generated SDL**.
5. **Assignments** are `publisher_distribution_platform` with composite primary
   key `(publisher_id, platform)`, retained disabled rows, an
   `enabled == (disabled_at IS NULL)` check constraint, a partial enabled
   index, and a `set_updated_at` trigger.
6. **The only row lock in the API** is
   `publisher_distribution_platform::crud::lock_publisher`, a
   `SELECT publisher_id FROM publisher WHERE publisher_id = $1 FOR UPDATE`
   taken as the first statement of both `PublisherDistributionPlatform::enable`
   and `::disable`. There is no other `FOR UPDATE` anywhere in
   `thoth-api/src`.
7. **Both BE-02 lifecycle functions take `&PgPool` and open their own
   transaction on their own pooled connection.** This is decisive for section
   7.6.
8. **Timestamps.** `diesel_manage_updated_at` installs a `set_updated_at
   BEFORE UPDATE` trigger, so `publisher.updated_at` moves on **any** publisher
   row update, including unrelated metadata edits performed by publisher users
   through `updatePublisher`.
9. **Pagination** is `limit`/`offset` with defaults `100`/`0`, a `*OrderBy`
   input object of `{field, direction}`, list-valued filters defaulting to
   `vec![]` meaning "no filter", and a sibling `*Count` query.
   `publishersByDistributionPlatform` additionally applies a mandatory
   `publisher_id ASC` tie-breaker so offset pagination is deterministic.
10. **Errors** are `ThothError`. `into_field_error` gives a distinct GraphQL
    `type` extension only to `InvalidSubjectCode` (`INVALID_SUBJECT_CODE`) and
    `Unauthorised` (message `Unauthorized`, type `NO_ACCESS`); every other
    variant falls through to `INTERNAL_ERROR`.
    `ThothError::DistributionPlatformNotAssignable` and
    `ThothError::EntityNotFound` already exist.
11. **Generated contract** is `thoth-client/assets/schema.graphql`, written by
    `thoth-client/build.rs` from `thoth_api::graphql::create_schema()`.

## 3. Explicit scope

The task must:

1. add the canonical service-configuration concurrency token specified in
   section 6, with its additive migration and manual `schema.rs` update;
2. add the `publisher_service_configuration_history` audit table and the closed
   audit-source type specified in sections 8 and 9;
3. add the protected `PublisherServiceConfiguration` representation specified
   in section 10;
4. add the owner-and-superuser single-publisher query specified in section 11;
5. add the superuser-only staff report and its count query specified in
   section 12;
6. add the superuser-only `replacePublisherServiceConfiguration` mutation
   specified in section 7, executing in exactly one database transaction;
7. refactor BE-02's assignment lifecycle additively so the mutation can reuse
   it inside a caller-owned transaction, as required by section 7.6, without
   changing BE-02's public behaviour;
8. add exactly one new `ThothError` variant and exactly one new
   `into_field_error` arm, per section 13;
9. regenerate the authoritative SDL, inspect the exact diff and update
   `thoth-client` artifacts where required, per section 14;
10. add the tests and evidence required by section 18;
11. update `docs/publisher-services/task-status.md` and `CHANGELOG.md`.

## 4. Non-goals

The task must not:

### BE-04 and jobs

1. create `distribution_job`, `distribution_job_target` or
   `distribution_job_attempt` tables, rows, or any placeholder or pseudo job;
2. expose fabricated, inferred or defaulted job status, including
   `NOT_STARTED` or `UNKNOWN` job states invented to satisfy a future UI;
3. add job-creation rules, a worker role, leases, claim tokens, retry or
   cancellation;
4. trigger any dissemination, upload, feed generation or back-catalogue work.

### Other programme tasks

5. perform or specify the MIG-01 historical backfill, or create any production
   assignment;
6. implement APP-01 or APP-02, or change `thoth-app` or
   `thoth-dissemination`;
7. implement LIC-01, LIC-02, OAI-PMH, EXP-01 or any Metrics surface.

### Surface and architecture

8. move `subscriptionPackage` onto the public `Publisher` GraphQL type, or make
   any package, capability or configuration value anonymously readable;
9. expose audit `before_state`/`after_state` JSON through any GraphQL surface;
10. expose adapter or feed identity, endpoints, buckets, hosts, account names,
    credentials, tokens or any operational deployment configuration;
11. change any of BE-02's four public read surfaces, their nullability, their
    ordering or their authorization;
12. introduce a second batching subsystem, a global or application-scoped
    loader, or a second loader equivalent to BE-02's existing
    publisher-assignment loader;
13. add a credential or configuration-secret table;
14. change mutation-guard mode, `OBSERVE`/`ENFORCE`, or take any action on PR
    [#799](https://github.com/thoth-pub/thoth/pull/799);
15. change workflow files or dispatch any workflow;
16. deploy, execute an environment or production migration, or access
    production.

## 5. Invariants

The implementation must preserve:

1. package and distribution-platform configuration remain independent: a
   package change never alters an assignment, and an assignment change never
   alters the package (ADR-0001);
2. `DistributionPlatform` and `MetricPlatform` remain separate with no
   conversion (ADR-0002);
3. the closed 17-value inventory with no `OTHER`, fallback or `Default`
   (ADR-0004);
4. BE-02's assignment lifecycle semantics exactly: retained disabled rows, new
   activation identity on re-enable, one transaction timestamp per transition,
   same-state no-ops that move no timestamp;
5. BE-02's OAPEN/DOAB normalization invariant, including repair of one-sided,
   split-activation and split-timestamp pairs;
6. `JISC_NBK` is included, inactive and non-assignable;
7. `OCLC_KB` and `EX_LIBRIS_KB` are independently configurable with no
   lifecycle coupling;
8. authorization fails closed;
9. PostgreSQL remains the sole durable owner of configuration and audit state
   (root `AGENTS.md` section 10);
10. `thoth-api/src/schema.rs` is edited manually and atomically with the
    migration (ADR-0003);
11. every existing public GraphQL surface remains backwards compatible.

## 6. Canonical optimistic-concurrency token

### 6.1 Decision

Add one dedicated column:

```sql
ALTER TABLE public.publisher
    ADD COLUMN service_configuration_updated_at timestamp with time zone
        DEFAULT CURRENT_TIMESTAMP NOT NULL;
```

Rust, appended to `Publisher` after `updated_at` so the `Queryable` column
order continues to match `schema.rs`:

```rust
// Snapshots and API payloads serialized before BE-03 lack this field;
// deserialization must not require it, so the documented default applies.
#[serde(default)]
pub service_configuration_updated_at: Timestamp,
```

GraphQL: exposed **only** as `PublisherServiceConfiguration.updatedAt`
(`Timestamp!`). No resolver is added to the public `Publisher` type, so adding
the struct field does not widen the public contract.

Mutation argument: `expectedUpdatedAt: Timestamp!`.

### 6.2 Update rule

Inside the mutation transaction, after the publisher row lock, the new token is

```sql
GREATEST(CURRENT_TIMESTAMP,
         <previous service_configuration_updated_at> + interval '1 microsecond')
```

This is required, not decorative. `CURRENT_TIMESTAMP` is
`transaction_timestamp()`: a transaction that started earlier but blocked on
the row lock commits later with an *earlier* timestamp. Equality against the
stored token still fails correctly in that case, so ordinary staleness
detection does not need monotonicity — but without the `GREATEST` rule a later
write could in principle store a value **equal to a token some client still
holds**, and that client's stale write would then wrongly succeed. The rule
makes the token strictly increasing per publisher and removes that ABA hazard
at no cost, because the previous value is already in hand from the locked read.

### 6.3 Rejected alternatives

- **`publisher.updated_at`.** Rejected on two independent grounds. It moves on
  *any* publisher row update because of the `set_updated_at` trigger, so an
  unrelated metadata edit — one a publisher user may perform through
  `updatePublisher` — would invalidate a superuser's in-flight configuration
  token. Conversely an assignment-only configuration change writes only
  `publisher_distribution_platform` and would **not** move it, so it fails the
  "changes on every committed service-configuration change" requirement
  outright.
- **Computed `max()` across publisher and assignment timestamps.** Rejected: it
  requires reading several independently changing timestamps after the lock
  check, gives the client a derived rather than stored token, cannot be
  compared in one atomic predicate against one stored column, and does not move
  at all for a change whose only effect is a package update on a publisher whose
  assignments are newer.
- **A separate `publisher_service_configuration` table holding one row per
  publisher.** Rejected as larger without being more robust: it needs a
  backfill for every existing publisher, a second lock target, and a
  create-on-first-write path, while providing exactly the same single-token
  semantics as one column on a row that is already the mutation's lock target.
- **A `bigint` version counter.** Functionally equivalent and marginally
  simpler to reason about, but rejected because the approved design specifies a
  timestamp-shaped `updatedAt`/`expectedUpdatedAt` contract, and section 6.2
  already gives the timestamp strict per-publisher monotonicity.

## 7. Mutation

### 7.1 Signature

```graphql
replacePublisherServiceConfiguration(
  data: ReplacePublisherServiceConfigurationInput!
): PublisherServiceConfiguration!
```

```graphql
input ReplacePublisherServiceConfigurationInput {
  publisherId: Uuid!
  subscriptionPackage: ThothPackage!
  enabledDistributionPlatforms: [DistributionPlatform!]!
  expectedUpdatedAt: Timestamp!
}
```

A single input object matches the repository's existing mutation convention
(`data: NewPublisher`, `data: PatchPublisher`).

The operation is **replace**, not patch: `enabledDistributionPlatforms` is the
complete desired enabled set, and `subscriptionPackage` is the complete desired
package. An empty platform list is a valid request meaning "no destination
enabled"; it is never interpreted as "unchanged" or "all".

### 7.2 Authorization

Superuser only, checked with the existing `PolicyContext::require_superuser`
before any read or write. A publisher user, including a `PublisherAdmin` of the
target publisher, is denied. There is no self-service configuration path in
BE-03.

### 7.3 Required transaction sequence

Exactly one database transaction on one connection:

1. `require_superuser()`. Deny before touching the database.
2. Acquire the publisher row lock — `SELECT publisher_id FROM publisher WHERE
   publisher_id = $1 FOR UPDATE` — as the first statement in the transaction.
   Absent row yields `EntityNotFound` and the transaction ends with no write.
3. Read the canonical current configuration under that lock: the publisher's
   `subscription_package` and `service_configuration_updated_at`, and the
   current enabled assignment set.
4. Compare `expectedUpdatedAt` with the stored
   `service_configuration_updated_at`.
5. If they differ, fail with the stale error of section 13. **No write, no
   version bump, no audit row.** This check precedes validation and diffing, so
   a stale request fails even when it would otherwise have been a semantic
   no-op.
6. Normalize the requested platform set: deduplicate, then close it under
   `DistributionPlatform::linked_members`, so naming either OAPEN or DOAB
   enables both and naming neither disables both. The normalized set — not the
   raw request — is what is diffed, persisted and recorded in the audit
   `after_state`.
7. Validate the normalized set. Any member that is not assignable — currently
   exactly `JISC_NBK` — fails with the existing
   `ThothError::DistributionPlatformNotAssignable` before any write.
8. Update `subscription_package` only if it differs.
9. Diff the normalized desired set against the current enabled set and apply
   the difference **through BE-02's domain functions**, per section 7.6. Never
   write `publisher_distribution_platform` rows directly.
10. If and only if step 8 or step 9 changed something, set
    `service_configuration_updated_at` per section 6.2.
11. If and only if step 8 or step 9 changed something, insert exactly one
    `publisher_service_configuration_history` row for the whole committed
    semantic change.
12. Commit, and return the configuration as it now stands, including the new
    token.

No distribution job is created. No external side effect occurs. No message,
file, feed or upload is produced.

### 7.4 Semantic no-op

A request whose token matches and whose normalized desired state equals the
current state is a success that writes nothing: the token does not move and no
audit row is written. Rationale: the token is a *configuration version*, and a
version that changes without a configuration change would force unnecessary
retries on every other client and would fill the audit table with rows whose
`before_state` and `after_state` are identical. The response is the unchanged
configuration.

This is consistent with BE-02, whose `enable`/`disable` already treat a
same-state transition as a no-op that moves no timestamp.

### 7.5 Independence

A request that changes only the package must leave every assignment row byte-
identical, including `activation_id`, `enabled_at` and `updated_at`. A request
that changes only platforms must leave `subscription_package` unchanged. Tests
in section 18.5 prove both directions.

### 7.6 Reusing BE-02 rather than re-implementing it

BE-02's `PublisherDistributionPlatform::enable` and `::disable` take `&PgPool`,
call `db.get()?`, and open their own transaction on their own pooled
connection. Calling either from inside the BE-03 transaction would therefore
run on a **different** connection and block on the publisher row lock that the
BE-03 transaction itself holds — a self-block that resolves only by timeout.

BE-03 must resolve this with an additive, behaviour-preserving refactor:

1. extract the existing bodies into connection-scoped functions taking
   `&mut PgConnection` and assuming a caller-owned transaction, for example
   `enable_on(connection, publisher_id, platform)` and
   `disable_on(connection, publisher_id, platform)`;
2. reduce the existing public `enable`/`disable` to acquiring a connection,
   opening a transaction and delegating, so their signatures, semantics,
   ordering, error behaviour and tests are unchanged;
3. have the BE-03 mutation call the connection-scoped functions inside its own
   transaction.

Re-acquiring the publisher row lock inside those functions is a harmless no-op
because the transaction already holds it, so the lock statement is left in
place rather than made conditional.

Re-implementing the lifecycle, the linked-group normalization or the
non-assignable check inside BE-03 is prohibited.

### 7.7 Lock ordering

The only row lock in `thoth-api/src` is BE-02's publisher-row lock. BE-03 takes
the same lock, on the same single row, as the first statement of its
transaction, and every subsequent read and write — publisher columns,
assignment rows, history insert — happens after it. The order is therefore
`publisher` row, then everything else, identically to BE-02, and a mutation
touches exactly one publisher, so there is no multi-object ordering and no
inversion is possible. Any future work that would lock an assignment row before
its publisher row must be treated as a lock-order conflict and stopped.

### 7.8 BE-04 seam

BE-04 will later extend **this same transaction**, between steps 9 and 12, to
create durable job rows atomically with the desired-state change. BE-03 must
therefore keep the mutation's transaction boundary explicit and its steps
separable, and must not push any part of the desired-state change outside the
transaction. BE-03 adds no job, no hook, no callback, no event and no
placeholder for that future work.

## 8. Audit history

### 8.1 Table

```sql
CREATE TABLE public.publisher_service_configuration_history (
    publisher_service_configuration_history_id uuid
        DEFAULT public.uuid_generate_v4() NOT NULL,
    publisher_id uuid NOT NULL,
    user_id text NOT NULL,
    source public.publisher_service_configuration_source NOT NULL,
    before_state jsonb NOT NULL,
    after_state jsonb NOT NULL,
    created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP NOT NULL,
    CONSTRAINT publisher_service_configuration_history_pkey
        PRIMARY KEY (publisher_service_configuration_history_id),
    CONSTRAINT publisher_service_configuration_history_publisher_id_fkey
        FOREIGN KEY (publisher_id)
        REFERENCES public.publisher(publisher_id) ON DELETE CASCADE
);

CREATE INDEX publisher_service_configuration_history_publisher_created_idx
    ON public.publisher_service_configuration_history
    USING btree (publisher_id, created_at DESC,
                 publisher_service_configuration_history_id DESC);
```

Notes:

- `user_id text NOT NULL` is exactly the existing `publisher_history`
  convention and holds `IntrospectedUser.user_id` from
  `PolicyContext::user_id()`. **No new actor identifier format is invented and
  no local account table is introduced.**
- actor and source are explicit columns, not fields buried in free-form JSON,
  so they are indexable, typed and greppable.
- `created_at timestamptz` follows BE-02. The legacy
  `publisher_history.timestamp` is `timestamp without time zone`; that defect is
  not copied forward and that table is not modified.
- the table is append-only: no `updated_at` column and therefore no
  `diesel_manage_updated_at` trigger.
- the composite index supports the section 12 latest-change lookup with a total
  order, so `DISTINCT ON` is deterministic even for two rows sharing a
  `created_at`.

### 8.2 Canonical audit state

`before_state` and `after_state` are the **bounded canonical service
configuration**, never a publisher snapshot:

```json
{
  "subscriptionPackage": "OASIS",
  "enabledDistributionPlatforms": ["OAPEN", "DOAB"],
  "configurationVersion": "2026-08-12T16:04:59.123456Z"
}
```

- `enabledDistributionPlatforms` is serialized in canonical
  `DistributionPlatform::ALL` declaration order, so two equal sets always
  serialize identically and a diff of two audit rows is meaningful;
- `configurationVersion` is the `service_configuration_updated_at` value before
  and after the change, which makes each row self-describing and lets a chain
  of rows be ordered without relying on `created_at` alone;
- the JSON must contain **no** credential, token, endpoint, bucket, host,
  account secret, adapter or feed identity, protected deployment configuration,
  or unrelated publisher metadata such as name, URL, `zitadel_id` or
  accessibility fields. Section 18.7 requires a test asserting the exact key
  set.

### 8.3 Write policy

- exactly one row per committed semantic change, covering the whole change,
  written inside the mutation transaction;
- a semantic no-op writes no row (section 7.4);
- a stale request writes no row;
- any failure rolls the row back with the rest of the transaction.

## 9. Audit source

### 9.1 Representation

A closed PostgreSQL enum plus the repository's standard derives, matching
`ThothPackage` and `DistributionPlatform`:

```sql
CREATE TYPE public.publisher_service_configuration_source AS ENUM (
    'SUPERUSER_API',
    'MIGRATION_BACKFILL'
);
```

- `SUPERUSER_API` - a committed `replacePublisherServiceConfiguration` call.
  This is the only value BE-03 can write.
- `MIGRATION_BACKFILL` - reserved for the separately approved MIG-01 controlled
  historical backfill/import. BE-03 defines the value and writes it never.

The Rust enum derives `diesel_derive_enum::DbEnum` and `juniper::GraphQLEnum`
with `ExistingTypePath` pointing at `crate::schema::sql_types::
PublisherServiceConfigurationSource`, exactly as BE-01 and BE-02 do. There is
deliberately no `OTHER`, no `UNKNOWN` and no `Default`: an unrecognised value
must fail rather than resolve to a nearest source.

The value identifies *how a change entered the system*. It must never encode a
hostname, container, task ARN, deployment identifier, environment name, account
or any other operational or secret identity.

### 9.2 Migration and versioning consequences

Adding a future source is an additive `ALTER TYPE ... ADD VALUE` migration.
Three consequences must be recorded in the implementation report and are stated
here so a future task is not surprised:

1. an enum value can be added but **never removed or renamed** without a type
   rebuild, so each new value is a permanent contract addition;
2. `ALTER TYPE ... ADD VALUE` interacts poorly with transactional migration
   runners: the repository applies migrations through the embedded
   `diesel_migrations` runner inside a transaction, and a newly added value
   cannot be *used* in the same transaction that adds it. A future
   source-adding migration must therefore add the value in its own migration,
   separate from any migration that writes it;
3. the value is GraphQL-visible on the superuser staff report, so adding one is
   an additive SDL enum change that downstream generated clients must tolerate.

## 10. Protected configuration type

```graphql
"The desired service configuration of one publisher."
type PublisherServiceConfiguration {
  "The publisher this configuration belongs to"
  publisher: Publisher!
  "Subscription package currently assigned to the publisher"
  subscriptionPackage: ThothPackage!
  "Distribution platforms currently enabled for the publisher, in canonical platform order"
  enabledDistributionPlatforms: [PublisherDistributionPlatformAssignment!]!
  "Version token of this configuration; supply it as expectedUpdatedAt to replace the configuration"
  updatedAt: Timestamp!
}
```

Decisions:

- `enabledDistributionPlatforms` reuses BE-02's existing
  `PublisherDistributionPlatformAssignment` type. No parallel protected
  assignment type is created, because the protected surface needs exactly the
  same fields BE-02 already exposes.
- `publisher: Publisher!` reuses the existing public type rather than
  duplicating publisher fields.
- the type carries **no** actor, no source, no audit JSON and no job state.
  Configuration-change metadata appears only on the superuser report (section
  12), which removes any need for conditionally-visible fields on this type and
  keeps it identical for owner and superuser callers.
- `ThothPackage` becomes SDL-reachable here for the first time (section 14.2).

## 11. Owner and superuser query

```graphql
publisherServiceConfiguration(
  publisherId: Uuid!
): PublisherServiceConfiguration!
```

### 11.1 Authorization matrix

| Caller | Result |
|---|---|
| anonymous | DENY - `Unauthorised` |
| authenticated, no role for the publisher's organisation | DENY - `Unauthorised` |
| publisher user scoped to another publisher | DENY - `Unauthorised` |
| publisher user scoped to this publisher (`PUBLISHER_USER`, `PUBLISHER_ADMIN`, or any publisher-scoped role satisfying the check) | ALLOW |
| superuser | ALLOW for any publisher |

Implementation: load the publisher, then
`PolicyContext::require_publisher_for(&publisher)`. That existing helper
short-circuits for superusers and otherwise requires the `PUBLISHER_USER` role
for the publisher's ZITADEL organisation. **No new ownership table, no new
role and no new authorization framework is introduced.**

"Own publisher" is therefore a per-publisher role check. An account holding
scoped roles for several organisations legitimately owns several publishers and
may read each of them; this follows the existing model rather than assuming one
publisher per account.

A publisher whose `zitadel_id` is `NULL` fails closed for every non-superuser,
because `PublisherId::zitadel_id` returns `Unauthorised`.

### 11.2 Not-found ordering

Loading the publisher precedes the role check, so an unknown `publisherId`
returns `EntityNotFound` to any authenticated caller. This mirrors the existing
`context.load_current` convention, which already requires authentication and
then loads. It is a deliberate, recorded choice: publisher existence is already
public through the existing anonymous `publisher`/`publishers` queries, so
distinguishing "absent" from "forbidden" discloses nothing new. Anonymous
callers are rejected before any load.

## 12. Superuser staff report

```graphql
publisherServiceConfigurations(
  publishers: [Uuid!]! = [],
  packages: [ThothPackage!]! = [],
  enabledPlatforms: [DistributionPlatform!]! = [],
  limit: Int = 100,
  offset: Int = 0,
  order: PublisherOrderBy = {field: "PUBLISHER_NAME", direction: "ASC"}
): [PublisherServiceConfigurationSummary!]!

publisherServiceConfigurationCount(
  publishers: [Uuid!]! = [],
  packages: [ThothPackage!]! = [],
  enabledPlatforms: [DistributionPlatform!]! = []
): Int!
```

```graphql
"A publisher's service configuration together with its latest change metadata."
type PublisherServiceConfigurationSummary {
  "The publisher's desired service configuration"
  configuration: PublisherServiceConfiguration!
  "Metadata of the most recent recorded configuration change, or null if none has been recorded"
  lastChange: PublisherServiceConfigurationChange
}

"Metadata of one recorded service-configuration change. The before and after states themselves are not exposed."
type PublisherServiceConfigurationChange {
  "When the change was committed"
  changedAt: Timestamp!
  "Identifier of the account that made the change"
  actor: String!
  "How the change entered the system"
  source: PublisherServiceConfigurationSource!
}
```

Rules:

- **superuser only**, enforced by `require_superuser()` at resolver entry,
  before any database access;
- empty list filters mean "no filter", per repository convention;
- `enabledPlatforms` matches publishers having an **enabled** assignment for
  **every** listed platform, so multiple values narrow rather than widen. The
  semantics must be stated in the field description;
- deterministic ordering: the requested `PublisherOrderBy` plus a mandatory
  `publisher_id ASC` tie-breaker, exactly as
  `publishersByDistributionPlatform` does, so offset pagination is stable;
- `lastChange` is nullable and is `null` for a publisher with no recorded
  change — for example every publisher immediately after the migration. It is
  **not** a fabricated placeholder;
- the count query applies exactly the same filter predicates as the list query.

### 12.1 Report boundary before BE-04

The report exposes: publisher, package, enabled platform state, configuration
version, and latest configuration-change metadata.

The report does **not** expose, and BE-03 does not implement: durable job status
filtering, latest back-catalogue job or attempt state, job failure state, or
pending-onboarding state. Those are BE-04 scope and are added later, additively,
to this same report. BE-03 must not depend on job tables that do not exist and
must not invent job state.

APP-02 therefore depends on **both** BE-03 and BE-04 for its final job-aware
report. APP-01 depends on BE-03 alone.

### 12.2 Query efficiency

For a page of N publishers the report must issue a bounded, constant number of
statements, with no per-publisher loop:

1. one set-based statement for the filtered, ordered, paginated publisher page,
   including `subscription_package` and `service_configuration_updated_at`;
2. one set-based statement for the latest change per publisher in the page —
   `DISTINCT ON (publisher_id) ... WHERE publisher_id = ANY($1) ORDER BY
   publisher_id, created_at DESC, publisher_service_configuration_history_id
   DESC` — eagerly attached to the row values by the resolver. Because it is
   fetched eagerly and reachable only from this superuser resolver, `lastChange`
   needs no DataLoader;
3. `PublisherServiceConfiguration.enabledDistributionPlatforms` must resolve
   through **BE-02's existing request-local
   `publisher_distribution_platforms` loader**, keyed by `publisher_id`. The
   key, the batch statement, the filtering and the canonical ordering are
   already exactly what the protected field needs, so per ADR-0007 and the
   prompt's reuse rule **no second loader is created**. Adoption must remain
   loader-first: the resolver registers its key at entry with no unrelated
   awaited work before `try_load`, uses `try_load` only, and inherits the
   foundation's total fail-closed batch behaviour, in which a backend failure
   produces an error for every key in the failed chunk with no retry and no
   per-parent fallback.

Required evidence (section 18.8): measured statement counts for the report at
representative page sizes — at least 1, 25 and 200 publishers — proving the
count does not grow with N, using the existing observed-loader test harness that
exercises the production batcher.

## 13. Error semantics

| Condition | Error | GraphQL `type` |
|---|---|---|
| anonymous or wrongly scoped caller | `ThothError::Unauthorised` (existing) | `NO_ACCESS`, message `Unauthorized` |
| publisher not found | `ThothError::EntityNotFound` (existing) | `INTERNAL_ERROR` (unchanged) |
| stale configuration version | `ThothError::StalePublisherServiceConfiguration` (**new**) | `STALE_SERVICE_CONFIGURATION` (**new arm**) |
| non-assignable platform requested | `ThothError::DistributionPlatformNotAssignable` (existing) | `INTERNAL_ERROR` (unchanged) |
| backend/database failure | `ThothError::DatabaseError` (existing) | `INTERNAL_ERROR` (unchanged) |
| invalid request shape | rejected by Juniper before the resolver | n/a |

Rules:

1. exactly **one** new `ThothError` variant and exactly **one** new
   `into_field_error` arm are added. The stale case needs a distinct machine-
   readable type so APP-01 can render a clear "configuration changed; reload"
   response rather than a generic internal error;
2. every other error family keeps its current mapping. In particular
   `DistributionPlatformNotAssignable` and `EntityNotFound` continue to surface
   as `INTERNAL_ERROR`. That is not ideal, but changing it would alter BE-02's
   merged contract and normalize unrelated error families, which is out of
   scope. It is recorded as a known limitation, not silently changed;
3. duplicates in `enabledDistributionPlatforms` are deduplicated, not rejected;
   the argument is a set;
4. the stale error message must state that the configuration changed and must
   **not** include SQL, table names, column names, driver text or the current
   stored token. Disclosing the current token to a caller who has just failed a
   version check would let a client blind-write over a change it never read.
   The caller re-reads the configuration through the query;
5. no new error path may log or return credentials, tokens, endpoints, buckets
   or personal data. The pre-existing `DatabaseError` variant already renders
   the underlying driver message; BE-03 must not add to that exposure, and this
   pre-existing behaviour is recorded rather than claimed to be absent.

## 14. GraphQL contract and client obligations

### 14.1 Exact additive inventory

Queries added: `publisherServiceConfiguration`,
`publisherServiceConfigurations`, `publisherServiceConfigurationCount`.

Mutations added: `replacePublisherServiceConfiguration`.

Object types added: `PublisherServiceConfiguration`,
`PublisherServiceConfigurationSummary`,
`PublisherServiceConfigurationChange`.

Input types added: `ReplacePublisherServiceConfigurationInput`.

Enums added: `PublisherServiceConfigurationSource`.

Enums becoming SDL-reachable: `ThothPackage`.

Nothing else. No scalar, no interface, no union, no field on an existing type,
and no change to any existing field's type, nullability, arguments, defaults or
description.

### 14.2 `ThothPackage` reachability

`ThothPackage` is currently **not** present in
`thoth-client/assets/schema.graphql`, because BE-01 added no GraphQL surface for
it. `PublisherServiceConfiguration.subscriptionPackage` makes it reachable, so
the SDL diff will contain a new `enum ThothPackage { OASIS OBELISK SPHINX
PYRAMID }` block. This is expected and must be confirmed in the SDL diff rather
than discovered later. It does **not** make package values anonymously
readable: reachability of a type is not exposure of a value, and every field
returning it is protected.

### 14.3 Obligations

1. regenerate the authoritative SDL by building `thoth-client`, which runs
   `build.rs`;
2. inspect the **exact** SDL diff and confirm it equals section 14.1 plus the
   `ThothPackage` block, with no unintended reordering, removal or description
   change;
3. commit the regenerated `thoth-client/assets/schema.graphql`;
4. update `thoth-client/assets/queries.graphql` and the client's generated
   types only if the client actually consumes a changed surface. BE-03 adds
   protected operations the internal export client does not use, so the
   expected outcome is **no query change**; if that holds it must be stated as a
   reviewed conclusion, not an omission;
5. build and test `thoth-client`;
6. assess `thoth-app` codegen compatibility as an additive-only change and
   record the conclusion. **BE-03 does not modify `thoth-app`**;
7. record the exact backend commit SHA of the reviewed BE-03 head in the
   implementation report, for later APP-01 contract pinning under the reserved
   BE-03/APP-01 contract control in the rollout plan.

## 15. Data and migration requirements

Migration required: YES

Schema changes:

- `ALTER TABLE public.publisher ADD COLUMN service_configuration_updated_at
  timestamptz DEFAULT CURRENT_TIMESTAMP NOT NULL`;
- `CREATE TYPE public.publisher_service_configuration_source`;
- `CREATE TABLE public.publisher_service_configuration_history` and its index.

`down.sql` drops the table, the type and the column.

BE-03 must **not** create `distribution_job`, `distribution_job_target`,
`distribution_job_attempt`, worker-role persistence, or any credential or
configuration-secret table.

`thoth-api/src/schema.rs` is updated manually and atomically in the same pull
request under ADR-0003 Architecture A: the new table, the new
`sql_types::PublisherServiceConfigurationSource`, and the new `publisher`
column appended after `updated_at` so `Queryable` column order continues to
match the `Publisher` struct.

Populated database behaviour: every existing publisher receives a
`service_configuration_updated_at` value from the column default. No package
value changes, no assignment row changes, and zero audit rows are created.

Locking/downtime assessment: the `ADD COLUMN` takes a brief `ACCESS EXCLUSIVE`
lock. The default `CURRENT_TIMESTAMP` is `STABLE`, not volatile, so PostgreSQL
is expected to store it as a fast default without rewriting the table. That
expectation must be **measured, not asserted**: compare `pg_class.relfilenode`
for `publisher` before and after the migration on a representative populated
database, and record the observed lock and duration.

Data backfill: none beyond the column default.

Idempotency: not applicable; the migration is a one-shot DDL migration.

Rollback or forward-repair strategy: see section 17.

Empty database test: apply, revert, re-apply cleanly.

Populated database test: forward migration against a representative populated
database, proving existing package values unchanged, existing BE-02 assignments
unchanged, zero audit rows created and zero distribution jobs created.

No production migration is part of BE-03.

## 16. Observability and operations

Required logs: none beyond existing request logging. The mutation must not log
the audit JSON, the actor identity, tokens or credentials.

Required metrics/alerts: none. BE-03 activates nothing to monitor.

Operational runbook changes: none. Deployment and migration execution remain
governed by CG-13 and separate release authorization.

## 17. Rollout and rollback

### 17.1 Rollout

- initial state after merge: **repository history changes only.** No deployment
  occurs, no migration is executed anywhere, no configuration changes and no
  API behaviour is available to any client;
- feature flag/configuration: none. A protected superuser-only mutation does
  not require a flag, and no repository or programme authority establishes one.
  Production deployment and migration execution remain separate explicit gates;
- staging/preview validation: after a separately authorized deployment and
  migration, the protected query and mutation capability exists. Even then: no
  automatic distribution job exists, no dissemination occurs, no publisher
  backfill occurs, no platform is automatically enabled, and existing package
  and platform state is unchanged;
- pilot: not applicable to BE-03; the first configuration change is an
  operational act requiring its own authorization;
- activation approval: automatic job creation remains disabled until BE-04.
  MIG-01 remains the controlled historical configuration backfill;
- observation period: not applicable.

### 17.2 Rollback

Before environment adoption — no deployment, no migration executed, no audit or
configuration data exists:

- an ordinary code revert of the pull request is possible under normal review.

After environment adoption — the migration has run and audit or configuration
data may exist:

- **retain the additive foundation.** Forward repair is preferred;
- an incorrect publisher configuration is restored through a reviewed and
  audited configuration change, which itself writes an audit row, not by
  editing or deleting history;
- **do not destroy audit history as routine rollback.** Dropping a populated
  `publisher_service_configuration_history` table requires separate explicit
  authorization;
- the `down.sql` migration is reversibility **evidence**, not an automatic
  production rollback procedure.

## 18. Acceptance criteria and required tests

### 18.1 Acceptance criteria

- [ ] one canonical concurrency token exists, is stored, and changes on every
      committed service-configuration change and on no other event;
- [ ] the mutation executes in exactly one transaction and is superuser-only;
- [ ] a stale write fails, writes nothing and creates no audit row;
- [ ] every committed semantic change writes exactly one audit row containing
      actor, source and bounded before/after canonical state;
- [ ] a semantic no-op moves no token and writes no audit row;
- [ ] the owner query satisfies the section 11.1 matrix exactly;
- [ ] the staff report is superuser-only, paginated, deterministically ordered
      and filterable by publisher, package and enabled platform;
- [ ] BE-02's public surfaces, ordering and authorization are unchanged;
- [ ] `subscriptionPackage` is not on the public `Publisher` type and no
      package value is anonymously readable;
- [ ] no audit before/after JSON is exposed through GraphQL;
- [ ] no distribution job, job target, job attempt or fabricated job status
      exists;
- [ ] the report's statement count does not grow with the number of publishers;
- [ ] the SDL diff equals the section 14.1 inventory plus `ThothPackage`;
- [ ] `feature/publisher-services/be-03` was created only under separate
      explicit implementation authorization.

### 18.2 Authorization and non-exposure tests

Required, each asserting the exact error:

- anonymous cannot read the protected configuration query;
- anonymous cannot read package through any route;
- an authenticated account with no role for the publisher cannot read it;
- an account scoped to another publisher cannot read it;
- an owner-scoped publisher account **can** read its own configuration;
- a superuser can read any publisher's configuration;
- an account scoped to two publishers can read both and no third;
- a publisher whose `zitadel_id` is `NULL` denies every non-superuser;
- a publisher user **cannot** mutate;
- a `PublisherAdmin` of the target publisher **cannot** mutate;
- another authenticated non-superuser cannot mutate;
- anonymous cannot mutate;
- a superuser **can** mutate;
- the staff report and its count query deny anonymous, owner and
  other-publisher callers and allow only superusers;
- the public `Publisher` SDL type contains no `subscriptionPackage` and no
  configuration-version field.

### 18.3 Concurrency tests

The canonical scenario, executed against a real database with two connections:

```text
clients 1 and 2 both read configuration version A
client 1 replaces with expectedUpdatedAt = A  -> succeeds, version becomes B
client 2 replaces with expectedUpdatedAt = A  -> fails as stale
client 2 wrote nothing
client 2 created no audit row
final state == exactly what client 1 committed
```

Also required:

- the same scenario where the two clients' changes touch **linked OAPEN/DOAB**
  assignments, proving the loser leaves no partial or one-sided pair;
- a stale request that would have been a semantic no-op still fails;
- concurrent mutations on **different** publishers do not contend;
- a mutation concurrent with a direct BE-02 `enable`/`disable` on the same
  publisher serializes on the publisher row lock and produces a consistent
  final state with no deadlock;
- token strict monotonicity per publisher across a sequence of committed
  changes.

### 18.4 Transaction and audit tests

- a failure injected after the package update and before commit rolls back the
  package, the assignments, the token and the audit row together;
- exactly one audit row per committed semantic change, never two;
- audit `before_state` equals the pre-change canonical state and `after_state`
  equals the post-change canonical state, both with platforms in canonical
  order;
- the audit JSON key set is exactly the section 8.2 keys — a test that fails if
  any additional key, including any publisher metadata or secret-shaped field,
  is ever added;
- `user_id` equals the authenticated caller's ZITADEL user id;
- `source` is `SUPERUSER_API` for every row BE-03 writes.

### 18.5 Independence and linked-platform tests

- a package-only change leaves every assignment row byte-identical, including
  `activation_id`, `enabled_at` and `updated_at`;
- a platform-only change leaves `subscription_package` unchanged;
- requesting OAPEN alone enables both OAPEN and DOAB with one shared activation
  and one shared timestamp;
- requesting DOAB alone does the same;
- omitting both disables both;
- an existing one-sided, split-activation or split-timestamp pair is repaired by
  a replace that includes the group, per BE-02's normalization invariant;
- `OCLC_KB` and `EX_LIBRIS_KB` are independently enabled and disabled with no
  coupling;
- requesting `JISC_NBK` fails with `DistributionPlatformNotAssignable`, writes
  nothing, moves no token and creates no audit row;
- duplicates in the requested list are deduplicated with no error;
- an empty requested list disables everything and is never treated as "all".

### 18.6 BE-02 regression tests

- BE-02's existing lifecycle tests pass unchanged against the refactored
  connection-scoped functions;
- BE-02's four public read surfaces return identical results, ordering and
  errors;
- the pool-based `enable`/`disable` public API behaviour is unchanged.

### 18.7 Migration and schema tests

- empty database apply, revert, re-apply;
- representative populated database forward migration, proving existing package
  values unchanged, existing BE-02 assignments unchanged, zero audit rows and
  zero distribution jobs;
- `pg_class.relfilenode` for `publisher` compared before and after, with the
  observed lock and duration recorded;
- catalog verification of the new type, table, primary key, foreign key,
  index and column;
- `thoth-api/src/schema.rs` matches the migrated database contract.

### 18.8 Query-count evidence

Measured statement counts, using the existing observed-loader harness that
exercises the production batcher:

- the report at 1, 25 and 200 publishers, each requesting
  `enabledDistributionPlatforms`, proving a bounded constant statement count
  and no per-publisher loop;
- the single-publisher query;
- confirmation that no second assignment loader was introduced.

### 18.9 Required commands

From an appropriate disposable test environment:

```bash
cargo fmt --all -- --check
git diff --check
cargo check --workspace
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo test -p thoth-api --features backend
cargo test -p thoth-client
cargo test --workspace
cargo run migrate
cargo run migrate --revert
```

Record exact commands and concise results. Never point tests at production or a
shared service.

## 19. Stop conditions

Return `BLOCKED`, identifying the exact decision or evidence required, if:

1. this specification is absent, unapproved or not repository-authoritative on
   `develop`;
2. implementation authorization is absent or not bound to a freshly verified
   exact `develop` head;
3. BE-01 or BE-02 is not actually merged and current on the verified base;
4. the current authorization architecture cannot express owner-only protected
   reads without a wider architectural change;
5. the BE-03/BE-04 phase boundary requires an architectural decision beyond the
   programme decision recorded in
   [`docs/publisher-services/decisions.md`](../../../publisher-services/decisions.md);
6. safe optimistic concurrency cannot be expressed with the single canonical
   token of section 6;
7. BE-02's locking and the section 7.7 lock order conflict materially — in
   particular if any code path locks an assignment row before its publisher
   row;
8. audit actor identity cannot be represented from existing authenticated
   account state;
9. the required report necessarily depends on BE-04 job persistence rather than
   permitting a later additive BE-04 extension;
10. a cross-programme abstraction or change is required;
11. implementation would require protected production configuration, secrets or
    production access;
12. any requirement would need action on PR #799 or on mutation-guard mode;
13. the SDL diff exceeds the section 14.1 inventory;
14. scope cannot be completed without unrelated changes.

## 20. Expected implementation report

The implementing agent must use
[`implementation-report-template.md`](../implementation-report-template.md) and
must additionally record:

- the exact verified base and implementation head;
- the exact SDL diff;
- the exact backend commit SHA for APP-01 contract pinning;
- migration evidence including the `relfilenode` comparison;
- the full authorization matrix results;
- the concurrency scenario results;
- measured statement counts;
- the audit JSON key-set assertion;
- confirmation that no job table, job row or fabricated job status exists.

## 21. Recommended execution

Implementation model: Claude Opus 5
Reasoning level: Extra High / xhigh
Independent reviewer: a fresh independent strong model and context that did not
implement, author, remediate or materially assist with BE-03
Review reasoning level: Extra High / xhigh

## 22. Branch and integration plan

- branch source: a freshly verified exact `develop` head at implementation time;
- pull-request target: `develop`;
- expected merge order: after this specification is repository-authoritative on
  `develop`, and before BE-04 and APP-01;
- parent programme branch refresh requirement: none; the programme uses no
  integration branch;
- branch deletion after merge: YES
- final programme PR required: NO
- final release path: `develop -> master`

## 23. Lifecycle boundary

This document is a specification. Its presence on `develop` makes BE-03's
requirements repository-authoritative; it does not create the implementation
branch, authorize an implementation edit, or activate anything.

BE-03 implementation status is `NOT AUTHORIZED`. The branch
`feature/publisher-services/be-03` must not exist until separate explicit CTO
authorization from a freshly verified base.

Live review, authorization and merge evidence for the pull request carrying this
content is the GitHub pull-request record and is not copied here (ADR-0005).

## 24. Approval boundary

Specification approval settles **what BE-03 must do**. It does not:

- authorize implementation;
- authorize deployment or environment migration execution;
- authorize production migration, assignment creation or backfill;
- authorize distribution activation or any `OBSERVE`/`ENFORCE` transition;
- authorize production access, credential use or workflow dispatch;
- start BE-04, MIG-01, APP-01 or APP-02.

## 25. Approval

Approved for implementation by:
Date:
Notes:

Record only the durable implementation authorization here. Independent review
decisions, CTO merge authorization and the merge itself are terminal GitHub
evidence under ADR-0005 and must not be copied back into this file.
